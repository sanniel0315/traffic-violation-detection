# 車速校正現場 SOP

> 目的：替相機設定「可信的測速校正」，讓超速能正確、準確地開單。
> 背景：系統政策「**只信校正來源開單**」預設開啟——**沒校正的相機不會開速度單**（速度仍顯示與記錄，只是不開單）。

## 何時需要做
- 相機有開「超速」偵測，但**沒有** trip-wire 兩線或 homography 4 點校正時。
- **目前待校正（2026-07 部署後）**：
  - `cam 2 台62基隆段隧道口`（超速開、無校正 → 已停開速度單）
  - `cam 8 62 交通流量`（超速開、無校正 → 已停開速度單）
- 其餘相機（62_1 / 台62_LPR / 國8）超速未啟用，不受影響、不需處理。

## 兩種校正方式（擇一即可，trip-wire 最準）
| 方式 | 原理 | 適用 | 準度 |
|------|------|------|------|
| **A. Trip-wire 跨線**（建議） | 畫兩條線、量兩線實距，用跨線時間差算速度 | 車輛直行等速的路段（隧道口、直線車道） | 最高（物理實測） |
| **B. Homography 4 點** | 一個梯形罩住車道，填實際寬長，像素轉真實公尺 | 路口/彎道也可 | 中高 |

## 最少必填 vs 選填（**沒設定的欄位一律套系統預設**）
現場只要把**必填**做完就能運作；**選填欄位留空 = 自動用預設值**，不必逐格填。

| 欄位 | 必填？ | 留空時的預設 |
|------|--------|--------------|
| Trip-wire 兩條線（in/out）+ 車道編號 + 兩線實距 | **必填**（A 方式） | 無預設，缺了不觸發 |
| Homography 4 點 + 寬 + 長 | **必填**（B 方式） | 可按「自動校正」帶預設假設值（寬 3.5m／長 10m） |
| 速限 `speed_limit` | 建議填 | 50 km/h |
| 容許超速 `overspeed_kmh` | 選填 | 10 km/h（超過「速限+10」才開單） |
| 車道寬 `calibration_width_m` | B 方式必填 | 3.5m（自動校正帶入） |
| 進階參數（平滑因子、離群、採樣數、Track 逾時…） | 選填 | 各有預設（0.35／2.0／5／5s…），現場**不用動** |
| 只信校正來源開單 `speeding_require_calibrated` | 選填 | **開（True）**——沒設定的相機一律視為「只信校正來源」 |

> 換句話說：**現場最省事的做法 = 只畫 trip-wire 兩線＋填車道編號＋兩線實距＋速限，其餘全部留空吃預設。**

---

## 出發前準備
1. **量距工具**：皮尺、測距輪，或現場已知間距的參考物。
2. **連線**：能開系統網頁（Tailscale 私網或現場網路皆可）。
3. **選路段**：在相機畫面中找一段「車輛直行、速度穩定、範圍完整入鏡」的路面。

---

## 方法 A：Trip-wire（建議，先做這個）

### A1. 現場量兩線實距
- 沿**行車方向**在路面選相隔一段距離的**兩個橫斷位置**（建議 **8~15 公尺**：太近時間差小、太遠一條會出鏡）。
- 用皮尺／測距輪量出兩位置的**實際距離（公尺）**，記下來（例：10.0）。
- 兩位置要**垂直於車流**、車輛會直線通過、相機都清楚看得到。
- 沒工具時可用路面標線輔助（但務必確認你採用的標線間距為當地實際值，勿用預設值猜）。

### A2. 畫兩條線
1. 網頁 → 選該相機 → **車速設定** → 「開啟 ROI 編輯器」。
2. 類型選「**車速進入線 speed_line_in**」，在畫面上點 2 點畫過**近端**橫斷位置。
3. 按「儲存區域」時，系統會**強制**跳出兩個輸入框：
   - **車道編號**：例如 `1`（進入線與離開線要填**相同**編號才能配對）。
   - **兩線實距(m)**：填 A1 量到的公尺數（例 `10`）。
4. 類型改「**車速離開線 speed_line_out**」，畫過**遠端**橫斷位置，儲存時填**相同車道編號**、**相同距離**。
5. 多車道：每個車道各畫一組 in/out，**車道編號不同**（1、2、3…）。

> 舊系統畫的線若當初沒填距離/車道，會沉默不觸發——重畫一次即可（現在會強制填）。

### A3. 設速限
- 回車速設定頁，該車速區/線設 **速限 `speed_limit`**、**容許超速 `overspeed_kmh`**（預設 10，即超過「速限+10」才開單）。

### A4. 存檔重啟
- 按「**完成（儲存並重啟服務）**」，偵測會重讀新設定。

---

## 方法 B：Homography 4 點（替代/補強）
1. ROI 編輯器畫一個「**車速區 speed_roi**」——4 點梯形，罩住一段車道。
2. 車速設定頁該區填：
   - **`calibration_width_m`**：車道**實際寬度**（台灣一般約 3.5m，以現場為準）。
   - **`calibration_length_m`**：梯形涵蓋的**實際路面長度**（量出來）。
3. 或按「**自動校正（消失點）**」一鍵產生 4 點＋寬長。
   - ⚠ 自動校正用的是**假設值**（車道 3.5m、路段 10m），**務必**用下方「驗證」步驟量誤差、必要時改回真實寬長。
4. 4 點 + 寬 + 長都齊 → 標籤顯示 Homography 啟用 → 按「完成（儲存並重啟）」。

---

## 驗證（**必做**，確認校正真的準）
1. 該相機**偵測功能**設定裡打開「**精度校正記錄**」開關 → 存檔並重啟。
2. 讓現場**真實車流**跑一段時間，收集 **≥30 台**跨越 trip-wire 的車。
3. 車速設定頁「**4. 精度校正報告**」→ 按「**產生／更新報告**」。
4. 判讀：
   - 應出現「**已校正 (homography)**」或 trip-wire 有值的那組，且 **n > 0**（修復前因舊 bug 恆為 n=0；出現代表校正生效）。
   - **MAE**（平均絕對誤差）越小越好；**偏差 bias** 正=系統高估、負=低估。
   - 「純 pixel」那組若列出「**建議係數**」，可按「**套用建議係數**」自動填回 `speed_kmh_per_pxps`。
5. MAE 過大時檢查：兩線實距量錯／畫線沒對齊橫斷／車道寬填錯／線畫在會變換車道的位置。

---

## 完成後
- 確認該相機在**真超速時**開始正常產生超速單。
- 若某台**暫時無法校正**又必須開單：車速設定頁把「**只信校正來源開單**」關掉 → 恢復純 pixel 開單（**準確度低、可能誤開**，僅作臨時措施）。

## 常見問題
| 症狀 | 原因 | 處理 |
|------|------|------|
| 畫了線卻不開單 | in/out 車道編號沒配對，或距離沒填 | 重畫（現在會強制填車道+距離） |
| 速度全空 (NULL) | 未校正 + 「只信校正來源開單」開 | 照本文校正，或臨時關該開關 |
| 速度飆到 100+ 不合理 | 純 pixel 抖動偽值 | 改用 trip-wire 或 homography |
| 報告「已校正」組 n=0 | 該相機沒有生效的 homography/trip-wire | 檢查 4 點+寬長是否齊、或 trip-wire 兩線是否配對 |

---
_對應系統版本：測速準確度 Tier 0-3（commit d4aded3）。UI 位置：偵測功能設定含「精度校正記錄」開關；車速設定頁含「只信校正來源開單」開關與「4. 精度校正報告」面板。_
