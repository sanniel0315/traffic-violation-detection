# 動態號誌控制 上線報告 — 骨架與舉證方法

> 目的：`我們隨時準備好，影子測試驗證演算法正常，邏輯比對報告優於現今，就要作報告上線`
> 這份文件先把「**哪些能證明、哪些不能**」講清楚，避免報告寫出撐不住的結論。
> 建立：2026-09-04。

---

## 〇、最重要的一件事：一致率不能拿來宣稱「更好」

目前驗收清單裡的「與現行控制一致率 ≥ 95%」是**安全門檻**，不是優越門檻。

- 一致率高 ＝ 我方的判斷跟 OPAC 幾乎一樣 ⟹ **接手不會亂來**（可以放心換人開）
- 一致率 100% ＝ 我方和 OPAC 完全相同 ⟹ **一點也沒有比較好**

所以「一致率達標」只能寫成「**驗證我方演算法在真實車流下不會做出離譜決策**」。
把它寫成「我方優於現行」是錯的，審查時會被打回來。

## 一、三類指標，各自能撐住什麼結論

| 指標 | 資料來源 | 能撐住的結論 | 撐不住的 |
|---|---|---|---|
| **有車一致率**、歧異方向、成本項 | 影子逐筆紀錄 | 演算法行為合理、不會亂切、歧異可解釋 | 「比較好」 |
| **成效基準**（總延滯／平均與最大排隊／回堵次數／切換頻率） | 我方排隊量測 + 實際切換事件 | 現行控制下的**真實表現**，是日後比較的基準線 | 「我方會更好」 |
| **A/B 交替時段成效** | 我方實際接管後的同性質時段對照 | **我方優於現行**（唯一能直接證明的） | — |

## 二、為什麼影子資料無法證明「更好」

我方沒有真的控制過，「如果當時是我方在控」是**反事實**，量不到。

拿影子決策去回推成效會踩到一個致命問題：**沒有回饋迴路**。
我方若在某一刻提早換相，之後的車流狀態就會與實際不同，
但我們手上只有 OPAC 那一條時間線的排隊量測。用它去餵我方的假想決策，
等於假設「我方換相不會改變後續車流」—— 那個假設本身就否定了控制的意義。

**結論：影子只能證明安全，不能證明優越。**

## 三、要證明「優於現今」的三條路

### A. 交替時段 A/B（最有說服力，建議走這條）

L5 分階段接管後，安排交替時段，比較同性質時段的成效：

```
第 1 日  06:00-09:00 我方控制    09:00-12:00 OPAC
第 2 日  06:00-09:00 OPAC        09:00-12:00 我方控制   ← 對調,消除時段效應
… 重複數日 …
```

- 對調是必要的：不對調就分不出「我方比較好」還是「早上本來就比較順」
- 比較欄位用 `/api/signal/shadow/outcome/compare`（已備妥）
- 需要 **L5 開通**，且中心端停用 OPAC 期間由我方控制

### B. 模擬驗證

建車流模型，用同一份到達流量餵兩套演算法。
⚠️ 模型必須先用現場資料校準過，否則只是把假設換個地方放。

### C. 不需反事實的窄範圍指標

有些指標可以直接比，不必假設：

- **綠燈空放**：綠燈期間該向排隊為 0 且流量為 0 的秒數佔比
- **最大綠撞頂次數**：達 max-green 被迫換相（代表配時不足）
- **回堵事件數**：下匝道排隊達儲車上限 80%

若我方的決策規則在數學上必然減少其中某一項，可以就那一項單獨論證。

## 四、報告骨架（欄位先留，等資料）

1. **摘要** — 一句話結論 + 三個關鍵數字
2. **系統與資料源** — 抄錄連線、訊框量、CKS 完整性、資料完整度（驗收清單第一組）
3. **演算法規格** — 決策規則、成本項、四道安全閘門、常數與時制基準
   （截自「動態號誌控制」頁的即時決策盤，該頁本來就是為了可稽核而做）
4. **安全性驗證** — 有車一致率逐時、歧異方向與成因、安全閘門觸發統計
   ⚠️ 標題就寫「安全性驗證」，不要寫成「效能驗證」
5. **現況成效基準** — OPAC 控制下的總延滯／排隊／回堵／切換頻率
6. **成效比較** — ⚠️ **A/B 資料到位前這一節留白**，不要用影子推估填
7. **上線計畫與回退** — 分階段、死人開關、資料失聯降級、回退條件與操作
8. **驗收清單** — 直接附「驗收清單」頁的實測結果（每項都有 evidence）

## 五、上線前必須清掉的項目

驗收清單目前（2026-09-04 15:xx）狀態：**未通過 0 / 待改善 3**

| 項目 | 現況 | 上線前要求 |
|---|---|---|
| 硬體位元極性實證 | 待確認 15/16 | 不擋上線（已改為不做判定，不會產生假警報） |
| 設定總覽可讀 | 10/11，缺設備日期時間 | 送一次 `0F52` 取回即可 |
| 與現行控制一致率 | 93.6%，門檻 95% | **要連續數日達標**，且歧異能逐條解釋 |
| TC3 下發封鎖 | ✅ 已於 2026-09-04 改回 QUERY_ONLY=1 | L5 開通時才解除，且分階段 |

> 🛑 下發封鎖那項是驗收清單抓出來的：現場 systemd 曾把兩層保險都關掉
> （`SIGNAL_TC3_CONTROL=1` + `QUERY_ONLY=0`），設定命令當時「送得出去」。
> 查下發紀錄 279 筆全是查詢、沒送過設定，路口沒被動過。
> 已用 drop-in `zz-queryonly.conf` 改回 QUERY_ONLY=1。

---

## 六、路徑 B（模擬驗證）實作結果 — 2026-09-04

**結論：以目前的量測，路徑 B 不可行。** 過程與證據如下，不是放棄，是找到真正的阻塞點。

### 6.1 校準關卡做成硬性的，全程沒有放寬

`/api/signal/shadow/simulate` 的流程是「先校準、再比較」，而且**校準沒過就不回傳比較結果**。
判準：MAE ≤ 12 公尺且相關係數 ≥ 0.5，兩項都過才算 usable。
四次迭代全程沒有動過這個門檻 —— 門檻是用來擋結論的，為了讓自己通過而放寬它等於把關卡拆掉。

### 6.2 三個有原則的修正（都是「用更多真實資料取代假設」）

| # | 修正 | 修正前 | 修正後（尖峰時段） |
|---|---|---|---|
| ① | 時變到達率取代單一中位數 | r = −0.006 / −0.04 | — |
| ② | 到達率分母改用**全部**紅燈時間（原本只採計有成長的區間，系統性高估） | MAE 57.0m、模擬平均排隊 66m vs 實際 17m | MAE **13.9m**、r **0.46**、模擬 16.3m vs 實際 17.4m |
| ③ | 飽和流改現場量測（原本用教科書 1800 vph） | 分相2 模擬 8.4m vs 實際 17.9m | **模型發散** —— 見下 |

### 6.3 決定性的發現：`queue_m` 不守恆

現場量到的兩個參數**互相矛盾**：

```
到達率  分相1 0.080   分相2 0.106  輛/秒   （紅燈期間排隊成長）
飽和流  分相1 0.216   分相2 0.166  輛/秒   （綠燈期間排隊消退 + 到達率）
```

分相2 的有效容量 ＝ 0.166 × 綠燈佔比 ≈ **0.083**，**小於**到達率 **0.106**
⟹ 模型必然無限累積（模擬排隊爆到 506m / 665m）。

但現實中排隊是**有界**的（該時段實際平均 17m）。兩者不可能同時成立。

**所以 `queue_m` 這個量測不守恆**：從紅燈成長推的「到達」與從綠燈消退推的「離開」對不起來。
推測原因：
1. ROI 只涵蓋部分路段，排隊超出視野會被截斷（實際最大值卡在 ~125m）
2. 它是「停等長度估計」不是「車輛計數」，公尺↔輛的換算（7 m/輛）在不同車種與間距下誤差大

⚠️ 這**不影響決策引擎**：決策只需要「哪一側比較塞」的相對量，而 `queue_m` 在綠燈期間
單調遞減（分相2：20.7→15.6→12.0→9.7→7.5→5.2→4.7 m），方向完全正確。
不能用的是「守恆流量模擬」，那需要進出車輛數平衡。

### 6.4 要讓路徑 B 可行，需要的是「計數」不是「長度」

`congestion_detector` 本來就在算 `flow_vpm`（通過流量計數，守恆），
但影子紀錄只存了 `queue_m` 沒存流量 —— 已於 2026-09-04 補上 `flow_vpm_1/2` 欄位。

**下一步**：累積至少一整天含尖峰的流量資料後，把模擬改建在**車輛計數**上
（到達＝上游計數、離開＝停止線計數），屆時守恆自然成立，校準才有機會通過。

### 6.5 現階段對報告的影響

- 第 6 節「成效比較」**仍然留白**，不要用未通過校準的模擬填。
- 路徑 A（A/B 交替時段）仍是最有說服力也最快能拿到結論的路。
- 路徑 C（綠燈空放、最大綠撞頂、回堵事件）**不需要守恆模型**，可以現在就做。

---

## 七、飽和流改實測值後的驗證 — 2026-09-04 21:11（**尚未有結論**）

### 7.1 改動內容

`a412fa7`（2026-09-04 20:29 部署）：決策引擎的飽和流從假設的 **1800 vph** 改成現場實測的 **501 vph**，
`change_cost` 從 **12.5 降到 3.48** —— 換相門檻降 2.6 倍，我方應該會更願意換相。

### 7.2 當晚立即比對的結果：**樣本與時段都不對等，不能判定**

| | 改動前（17:30–20:29） | 改動後（20:35–21:11） |
|---|---|---|
| 可比對樣本 | 1,512 | 303 |
| **有車樣本** | **950** | **120** |
| 有車一致率 | 94.8% | 93.3% |
| 歧異（我方切／實際續綠） | 49（3.2%） | 8（2.6%） |
| **有代價的綠燈空放** | 28 筆（1.3%） | **0 筆** |

**不能用這組數字下結論**，三個理由：

1. **樣本量差 8 倍**（有車 120 vs 950）
2. **時段完全不同** —— 改動後是夜間 21 點，車流稀少
3. **目標指標在夜間根本不會發生** —— 「有代價的綠燈空放」需要「紅側有人在等」，
   夜間 0 筆，這次改動想改善的東西**沒有出現的機會**

93.3% vs 94.8% 的差距（1.5pp）在 120 筆樣本下沒有意義。
說「沒變差」和說「變差了」都是過度解讀。

### 7.3 正確的比對方式：同時段跨日

今天（09-04）尖峰跑的是**舊參數**，明天（09-05）尖峰跑的是**新參數** ——
同一時段對同一時段，才排除得掉時段效應。

**今日尖峰基準（06:00–09:00，舊參數 1800 vph）已固定**：

| 指標 | 數值 |
|---|---|
| 樣本 / 可比對 / 有車 | 2,106 / 1,557 / 965 |
| **有車一致率** | **93.2%** |
| 判定交叉 | KEEP·KEEP 1,490 ／ **SWITCH·KEEP 63** ／ KEEP·SWITCH 4 |
| **有代價的綠燈空放** | **346 筆（16.4%）＝ 1,730 秒** |
| 　我方判應換相 | **52 次（15.0%）** |
| 　KEEP 原因 | 不值得切 224 ／ 未滿最小綠 70 |
| 最大綠撞頂 ／ 回堵 | 0 ／ 0（門檻不可及） |

### 7.4 明日要看什麼

| 觀察點 | 預期方向 | 判讀 |
|---|---|---|
| 有代價的綠燈空放「我方判應換相」佔比 | 從 **15.0%** 上升 | 這是改動的**目的**。沒上升代表門檻不是瓶頸，要另找原因 |
| 有車一致率 | 維持在 **93.2%** 附近 | 掉到 88% 以下且歧異全是「我方切／實際續綠」＝ 門檻降過頭變成亂切 |
| KEEP 原因「不值得切」 | 從 **224** 下降 | 直接驗證 `change_cost` 降低是否真的解開了那些卡住的判斷 |

⚠️ 跨日比較的風險：天候、事故、上游施工都會讓兩天的車流不同。
若兩天差異很大，這組比較也不算數 —— 屆時要退回**同日交替時段**（路徑 A），
那需要 L5 開通。

### 7.5 期中比對（09-05 07:55）：**新參數根本沒在跑，這組比對作廢**

原本要在 09-05 09:10 做同時段跨日比對。07:55 先拉一次期中數字，
第一眼就看到不對：

| 指標 | 09-04 舊參數 | 09-05「新參數」 |
|---|---|---|
| 樣本 / 可比對 / 有車 | 1,369 / 1,021 / 587 | 1,362 / 1,019 / 450 |
| 有車一致率 | 92.3% | 86.9% |
| KEEP 不值得切 | 345 | 227 |
| 平均排隊 | 21.35 m | 9.94 m |
| **change_cost 平均** | **12.5** | **12.5** |

`change_cost` 兩天**都是 12.5** —— 新參數（501 vph → 3.48）沒有反映在 09-05 的任何一筆紀錄裡。
再看逐時分布：新參數只在 **09-04 20:00 → 09-05 01 時**生效過（值 1.5～3.5），
02 時起全部回到 12.5，`/plan` 也顯示 `saturation_source: default`。

**真因**：量到的飽和流只存在記憶體。09-05 凌晨連續四次部署、每次重啟 `traffic-api`，
重啟後用「最近 6 小時」重量 —— 半夜量出 116 / 137 vph，低於下限 200 被判無效，
於是退回預設 1800；之後每小時重量都還是半夜資料，永遠回不來。早尖峰整段跑假設值。

另外兩件事讓這組比較本來就不該做：
- 09-04 是**週五**、09-05 是**週六**。平均排隊 21.35 m vs 9.94 m、有車樣本 587 vs 450，量級不同。
- 兩天的「有車一致率」差 5.4pp，但因為兩天跑的是**同一組參數**，這個差距只反映車流不同，跟演算法無關。

**已修**（`signal_shadow.py` / `signal_sim.py`）：
1. 量到就寫 `data/signal_saturation.json`，啟動時載回 —— 重啟不再歸零
2. 重量視窗改 24 小時，且**只從「綠燈開始時至少排了兩台車」的段量** ——
   飽和流的定義就是停等隊伍的消退速率，半夜沒隊伍的綠燈本來就不該算
3. 逐相合併：相 1 量到、相 2 沒量到，只更新相 1，不再整個 dict 覆蓋把另一相打回預設
4. 第一版的 save/load 因為 `json` 沒 import 而 `NameError`，被 `except: pass` 吞掉 ——
   測試抓到的。改成記進 `_stats["last_error"]`，不再靜默

**下一步**：新參數要真的連續跑過一個工作日尖峰才有得比。
最早可比的是 **09-08（週一）06:00–09:00 vs 09-04（週五）同時段**；週末車流不能當基準。

### 7.6 精確比對方法：逐次綠燈配對（`/api/signal/shadow/paired`）

取樣層級的「一致率」只回答「同一秒兩邊說的一不一樣」，答不了控制上真正有意義的問題：
**每一次實際綠燈，我方第一次判 SWITCH 落在第幾秒，跟實際換相差多少。**
長綠燈還會灌樣本 —— 一段 100 秒的綠燈在一致率裡的權重是 20 秒那段的 5 倍。

改以**一次綠燈為單位**配對，每段算：

| 欄位 | 意義 |
|---|---|
| `actual_sec` | 實際綠燈長度（區間量測，真值在 [值, 值＋5s)） |
| `ours_sec` | 我方第一次判 SWITCH 時的已亮秒數；整段 KEEP 則無 |
| `delta_sec` | `ours − actual`，**負值＝我方會早切** |
| `red_waiting` | 我方判切那一刻紅側有沒有排隊 —— 有才是**有意義的早切** |
| `waste_sec` | 紅側有排隊時，我方判切到實際換相之間的秒數（有代價空放） |

分類：早切（其中 有意義／沒車）、同時（容忍一個取樣週期 5 s）、續綠（整段同意）、晚切。
段內或段前有斷點的段標 `truncated` 不計入；清道期間的 SWITCH 不算第一次判切。

**09-04（週五）06:00–09:00 舊參數 1800 vph 的配對基準：**

| 指標 | 數值 |
|---|---|
| 可用綠燈段 / 斷點排除 | 277 / 6 |
| 早切（全部紅側有車） | **19**（6.9%） |
| 同時 / 續綠 / 晚切 | 8 / 250 / 0 |
| 早切秒數 中位 / p10 / p90 | **−10.4 s** / −20.2 s / −5.1 s |
| 有代價空放合計 | **273 s** |
| 平均實際綠燈 | 依 by_phase |

判讀：舊參數下我方在 277 次綠燈裡只有 19 次會比 OPAC 早切，早的幅度中位 10 秒，
加起來 273 秒的紅側等待是我方認為可以省下的。**新參數的目標**就是這兩個數字：
早切次數（有意義）應上升、每次早切幅度應加大，同時「晚切」維持 0（我方不會比 OPAC 更慢放人）。

參考（不可當基準，週六）：09-05 06:00–08:02 舊參數 184 段／早切 14／代價 339 s；
09-05 08:02 起新參數 135 段／早切 5／代價 60 s —— 週六上午車流本來就少，數字只證明端點能跑。

**09-08（週一）正式比對時，以本節的配對指標為主，§7.4 的三個觀察點為輔。**

### 7.7 比對時段改為每日 09–12、17–20（使用者定義的尖峰）

使用者指示：每天早上 09–12、晚上 17–20。所有比對、UI 快捷區間、每日回報一律用這兩個視窗。

**09-04（週五）舊參數 1800 vph 的基準（配對法為主、取樣法為輔）：**

實際綠燈秒數取自**控制器每秒回報的 5F03 框**（`source=controller_5F03`，精確到 1 秒），不是影子 5 秒取樣的近似值。

| 視窗 | 綠燈段 | 早切（有車） | 同時 | 續綠 | 晚切 | 早切平均 Δ | 有代價空放 | 續綠裕度平均 | 續綠時紅側有車 | 取樣有車一致率 | 平均排隊 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 09–12 | 293 | **14**（4.8%） | 9 | 270 | 0 | **−16.1 s** | **225 s** | 179.7 車·秒 | 33% | 91.3% | 16.8 m |
| 17–20 | 293 | **5**（1.7%） | 13 | 275 | 0 | **−21.0 s** | **105 s** | 228.1 車·秒 | 52% | 93.8% | 20.1 m |

分相拆解：09–12 早切 分相1 9 次／分相2 5 次，代價 106 s／119 s；17–20 早切全在分相 1（5 次，105 s）。
綠燈平均：09–12 分相1 20.5 s／分相2 30.9 s；17–20 25.3 s／24.8 s。

> 🛑 這組數字比先用取樣法算的（早切 31／17）**少**。取樣法用影子自己的 5 秒取樣切段，
> 段尾最多晚 5 秒才知道換相，我方在那 5 秒內說的 SWITCH 會被算成「早切」——
> 其實控制器已經換了。改用控制器秒數後這些假早切被剔除。**以本表為準。**

**續綠段的比對數據**（`hold_compare`）：整段我方沒說 SWITCH 的綠燈，記 OPAC 切相那一刻我方的裕度
（門檻 − 紅側延滯）、紅側有沒有車、有沒有撞最大綠。裕度越大＝我方越堅定認為還不該切；
「紅側有車卻仍續綠」的比例＝我方比 OPAC 更保守的量（09–12 為 33%、17–20 為 52%）。

判讀方向（新參數應該出現的變化）：早切次數上升、平均 Δ 更負、代價空放合計上升（我方判斷「可以省下」的紅側等待變多），
**晚切維持 0**，取樣有車一致率不掉到 88% 以下。

跨日可比性：先比平均排隊與有車樣本數，差 30% 以上就註明不算數。週末不當基準。

**最大綠改固定 100 秒（09-05 使用者指示）**：時制表的 210 秒是控制器硬上限，不是操作上限。
我方引擎的最大綠閘門改為 100 秒（`SIGNAL_MAX_GREEN_SEC`，設 0 退回時制表）。
09-04 基準是在 210 秒下算的；09-04 兩視窗綠燈最長 114 s，改 100 秒後這類段會多出幾次「早切」，比對時要記得這一項。

**每日回報流程（自動）**：12:10 與 20:10 各跑一次當日視窗，對照同視窗的 09-04 基準，結果追加到 §7.8 的日誌表並 commit。

### 7.8 每日比對日誌

| 日期 | 視窗 | 參數 | 綠燈段 | 早切 | 同時 | 續綠 | 晚切 | 平均 Δ | 代價空放 | 有車一致率 | 平均排隊 | 可比？ |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 09-04 五 | 09–12 | 舊 1800 | 293 | 14 | 9 | 270 | 0 | −16.1 | 225 | 91.3% | 16.8 | 基準（控制器秒數） |
| 09-04 五 | 17–20 | 舊 1800 | 293 | 5 | 13 | 275 | 0 | −21.0 | 105 | 93.8% | 20.1 | 基準（控制器秒數） |
| 09-05 六 | 09–12 | 新 實測（飽和流 ~480、損失 5.0、每車 6.0、最大綠 100） | 295 | **19** | 6 | 270 | 0 | **−16.7** | **318** | **88.6%** | 17.7 | 週六、框距 3.06 s（精度一個框距）；量級可比（通過 +1%、排隊 +5%） |
| 09-05 六 | 17–20 | 新 實測（飽和流 ~499、損失 5.0、每車 6.0、最大綠 100） | 280 | **9** | 15 | 256 | 0 | **−15.0** | **135** | **91.0%** | 22.8 | 週六、框距 3.06 s（精度一個框距）；早切 9 全部紅側有車；續綠段裕度 174.9、OPAC 切相時紅側有車 41.8%；逐時一致率 17 時 90.8% / 18 時 95.1% / 19 時 87.2%，歧異 85 筆全為我方切／實際續綠；成效 A/B（09-04 vs 09-05 同視窗，皆 OPAC 控制，差異為路況）：延滯 5.01→4.25 s/車（p=.004, d=−0.24）、排隊 10.17→5.86 m（p<.001, d=−0.63）、通過 1617→1228 輛/h（p<.001, d=−0.81）、95 分位延滯 12.22→7.79；通過車數與基準差 24%、平均排隊差 42% → 排隊量級不可比，只記錄不當基準 |
| 09-06 日 | 09–12 | **新 keep_weight=3.0**(飽和流實測、損失 5.0、每車 6.0、最大綠 100) | 271 | **4** | 8 | 258 | **1** | **−23.1** | **92.5** | **94.6%** | 21.4 | 🛑 **週日,只記錄不當基準**;keep_weight 於 09-06 02:13:06 由 1.0 改 3.0,**與 09-04/09-05 是不同參數,不可直接比較**。樣本涵蓋率 97%(09時98%/10時96%/11時97%)。框距 3.09 s(精度一個框距)。取樣法有車 918 筆、一致率 94.6%、歧異 50 筆全為我方切／實際續綠、反向 0。續綠段裕度 73.2、切相時紅側有車 39.1%。**首次出現晚切 1 次**(10 時),未達回退門檻(任一小時 ≥3 或連續兩小時各 ≥1)。**驗收規格的績效指標**(R34 16613:東向出口匝道等候隊列長度 = 分相2):平均 6.85 m、最大 59.0 m。逐時一致率 09時 91.6% / 10時 93.9% / 11時 97.6% |

　　成效 A/B（09-04 vs 09-05 同視窗，皆 OPAC 控制、我方未下發，差異是兩天路況不是演算法）：延滯 −0.22 s/車（p 0.30）、排隊 −1.16 m（p 0.002，small）、通過 +10 輛/h（p 0.80）、最大排隊 −4.1 m（p<0.001）。
　　逐時：09 時一致 93.5%／10 時 **81.2%**（Δ −32.3，代價 194 s）／11 時 91.1%。續綠裕度 169.6，續綠時紅側有車 45%（基準 33%）。112 筆歧異全是「我方切／實際續綠」，「我方續綠／實際切」0。

### 7.9 成效指標規格（工程／技術／完整）與 09-04 基準

端點 `/api/signal/shadow/report?since&until[&b_since&b_until]&tier=min|standard|full`；模組 `detection/signal_eval.py`。
統計單位是**控制器 5F03 重建的號誌週期**（用 5 秒取樣當樣本會高估自由度，p 值假小）。

| 等級 | 指標 | 來源 / 方法 | 標示 |
|---|---|---|---|
| 工程 | 平均延滯 | 停等車數 × 取樣間隔積分 ÷ 通過車數（停等延滯，不含減速延滯） | approx |
| 工程 | 平均排隊長度 | 停止線相機 5 秒取樣 | **measured** |
| 工程 | 每小時通過車輛數 | 停止線相機通過事件（traffic_events） | **measured** |
| 技術 | 平均旅行時間 | 進場道長度 ÷ 區間車速 ＋ 停等延滯（LPR 跨相機再辨識 3 小時僅 3 組，不採用） | approx · 低信心 |
| 技術 | 平均停車次數 | 停等車·秒 ÷ 在場車·秒（0~1，無逐車軌跡） | approx |
| 技術 | 95 分位延滯 | 逐週期平均延滯的 95 分位（週期層級） | approx |
| 完整 | 進階 7 項 | 最大排隊、回堵週期數、週期長度平均／標準差、綠燈秒數、綠燈利用率（綠燈期間有車比例）、區間車速 | measured |
| 完整 | 統計 | Welch t-test、p、Cohen's d（<0.2 negligible／<0.5 small／<0.8 medium／≥0.8 large） | — |
| 完整 | 分時段 | 尖峰（09–12、17–20）vs 離峰 | — |

**09-04（週五）舊參數 tier=full 基準：**

| 視窗 | 週期 | 延滯 s/車 | 排隊 m | 通過 輛/h | 旅行 s | 停車率 | p95 延滯 | 最大排隊 | 回堵週期 | 週期 s | 綠燈 s | 車速 km/h |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 09–12 全部 | 293 | 4.61 | 7.91 | 1,228 | 11.3 | 0.68 | 9.7 | 77 | 0 | 73.4±26.0 | 25.7 | 34.5 |
| 　分相 1 | 146 | 5.63 | 7.58 | 944 | 15.9 | 0.66 | 10.8 | 77 | 0 | 73.4±25.2 | 20.5 | 37.6 |
| 　分相 2 | 147 | 3.60 | 8.24 | 1,510 | 7.2 | 0.70 | 6.6 | 72 | 0 | 73.4±26.8 | 30.9 | 31.7 |
| 17–20 全部 | 294 | 5.01 | 10.17 | 1,617 | 9.7 | 0.72 | 12.2 | 102.5 | 0 | 73.1±29.1 | 25.0 | 36.9 |
| 　分相 1 | 147 | 5.55 | 11.57 | 1,664 | 11.5 | 0.71 | 15.5 | 102.5 | 0 | 73.1±31.3 | 25.3 | 43.2 |
| 　分相 2 | 147 | 4.46 | 8.77 | 1,571 | 7.7 | 0.73 | 9.4 | 70 | 0 | 73.0±26.7 | 24.8 | 30.6 |

管線自檢（同日早晚互比，不是新舊參數）：晚尖峰通過 +390 輛/h（p<0.001，d=0.74 medium）、排隊 +2.3 m（p=0.0001，d=0.33 small）、
延滯 +0.4 s/車（p=0.15，不顯著）。檢定會動、方向合理。**新舊參數的正式 A/B 從 09-08（週一）起，以此表為 A。**

### 7.10 控制器燈態回報週期在 09-04 14:22:48 被改掉（影響精度，需回復）

抄錄框的 5FEF（燈態步階傳輸週期回報）顯示：**09-04 14:22:48 起 TransmitCycle 由 1 變 0**（TransmitType 維持 2）。
之後 5F03 從每秒一框（3,600/h）降到約 500/h、框距中位 1.8～6.6 秒。我方從未送出 5F6F 設定命令（QUERY_ONLY=1），
那一小時也沒有中央下傳紀錄 —— 是我方通道以外改的。

影響：
- 「控制器秒數」配對法的精度 = 一個框距。09-04 09–12 仍是 ±1 秒；**09-04 17–20 之後的基準精度要照 `precision_note` 解讀**。
- 損失時間用框量會被灌水：實測量到 8.5 秒（真值 3＋2＝5），並把 `change_cost` 從 3.5 灌到 10.6。
  已加守門：框距 >1.5 秒不用框量，改用控制器回報的時制設定 5FC4（黃燈＋全紅）；/plan 與 /paired 都帶 `frame_interval_sec`。

決策參數現況（/plan constants）：飽和流 實測 527 vph；損失時間 5.0 s（5FC4）；每車 6.0 m 實測（20,082 筆）；
最大綠 固定 100 s；回堵比例 80%（設定值）。`change_cost` 3.66。

**建議**：請控制器端把傳輸週期設回 1 秒（5F6F TransmitType 2、TransmitCycle 1）。我方可以下這一則設定，
但目前 QUERY_ONLY=1，要使用者明確同意才會開。

> **攝影機名稱對照**(全文一律用現場名稱,不用 camN):
> NE-1 = `CCTV-N8-E-9-L-NE-1-SIG`(上匝道上游)、NE-2 = `CCTV-N8-E-9-L-NE-2-SIG`(上匝道停等區,分相1 基準測點)、
> WN-1 = `CCTV-N8-E-9-L-WN-1-SIG`(下匝道上游)、WN-2 = `CCTV-N8-E-9-L-WN-2-SIG`(下匝道停等區,分相2 基準測點)。

### 7.11 驗收基準改為公認控制,與 09-05 發現的三個量測問題

使用者 2026-09-05:「我一定要有演算法,並且不是跟現在的比」「OPAC 是最差的控制,
沒法驗收」。驗收比較因此全面改為對照交通工程的公認控制,OPAC 只保留校準用途。

**對照組**(`detection/signal_baselines.py`,端點 `/api/signal/shadow/benchmark`):
固定時制(現行時制表 35/40)、Webster 最佳固定時制、感應控制(gap-out 3 s)、
MaxPressure。五套控制跑同一份實測到達流量、同一組安全約束(最小綠/最大綠/
損失時間由模擬器統一強制),差異只來自換相決策。校準沒過一律不給比較數字。

**問題一:飽和流被系統性低估約 2.3 倍(已修,commit 27c40d0)**
`estimate_saturation` 的分母用整段綠燈,但飽和流的定義是「停等隊伍存在時」的
放行速率。09-05 17-20 實測 167 段有效綠燈,隊伍在綠燈前 43~48% 就清空(中位數):

| 分母 | 分相1 | 分相2 |
|------|-------|-------|
| 整段綠燈(舊) | 457 vph | 341 vph |
| 飽和段(新) | 1045 vph | 797 vph |
| 加上到達率後的飽和流 | 1237~1407 vph | 1045~1171 vph |

同時排除「相機有量測下限」的假設:綠燈末 10 秒排隊中位數 0.0 m、七成低於 1 m。
連鎖後果:換相成本被低估約 3 倍(3.03 vs 應有約 8.8),引擎過度願意換相 ——
這可能就是有車一致率一路掉到 88% 的原因;流量比 y=q/s 被高估,模擬器排隊
無界成長,校準必然失敗。修正後 MAE 從 223.5/577.4 m 降到 9.9/11.7 m。

**問題二:校準仍未過,卡在相關係數(未解)**
修正後 MAE 已在門檻內(門檻 12 m),但相關係數只有 0.19/0.15(門檻 0.5)。
到達率的時間解析度不是原因 —— 視窗 900→120 秒逐一試過,900 秒最好,更短更差
(段內紅燈數不足,率的估計本身變雜訊)。**校準沒過就沒有比較數字**,
`/benchmark` 目前回「校準未通過」,這是設計上要的行為。

**問題三:通過車輛數算錯(2026-09-06 已修,舊值偏高約 2.5 倍)**

根因不是「重複計數」那麼單純 —— `traffic_events` 混了三種列,先前不分 direction
全部計入:

| direction | NE-2 三小時筆數 | 是什麼 |
|-----------|----------------|--------|
| IN / EXIT | 887 / 873 | **每車一筆的通過紀錄**(兩者應該相當,實際也相當) |
| INOUT | 686 | 每幀每車的偵測紀錄(同一台車在框內每秒可寫約 3 筆) |
| straight | 687 | **別的進場道**(NE-2 的 straight 是下匝道後平面道路) |

改成只取 `direction='EXIT'`(離開停等區 = 通過停止線)後:

| 相機 | 舊算法 | 只取 EXIT |
|------|--------|-----------|
| NE-2(分相1) | 1044 vph | 291 vph |
| WN-2(分相2) | 1358 vph | 361 vph |
| 合計 | 2403 vph | 652 vph |

新值在物理上站得住:該相飽和流 1184/909 vph、綠燈只佔約四成,容量約 530/380 vph,
舊值遠超容量、新值低於容量;也與紅燈排隊成長法估到的到達率(515 vph)量級相符。
🛑 **§7.9 與 09-04/09-05 每日報告裡的「通過車輛數」(09-12 1228、17-20 1617 輛/h)
是舊算法的值,偏高約 2.5 倍且混入別的進場道,不可引用。**
某台相機在某段時間沒有 EXIT 列時一律回空(量不到),不退回全部列湊數字。

**(原始記錄)問題三的初步觀察 —— 重複計數**
`traffic_events` 同一台車會產生多筆。09-05 09-12 UTC(本地 17-20)NE-2 有
連續三筆間隔 0.4~0.5 秒、bbox 幾乎重疊的 car 事件;同一秒內 2 筆以上的秒數
佔比很高(2 筆 413 秒、3 筆 104 秒、最多 12 筆)。以「同相機同車道間隔 ≥1.5 秒
才算一台」粗略去重:

| 相機 | 原始 | 去重後 |
|------|------|--------|
| NE-2(分相1 停止線) | 1044 vph | 559 vph |
| WN-2(分相2 停止線) | 1358 vph | 590 vph |

去重後的通過量與修正後的飽和流互相吻合(分相1 飽和流 1237 vph × 綠燈佔比
約 45% ≈ 557 vph,實測去重 559 vph),原始值則對不上。
🛑 **§7.9 與每日報告裡的「通過車輛數」(09-12 1228、17-20 1617 輛/h)是原始值,
偏高約 2 倍,引用前要先處理去重。** 去重規則要先確認(可能與偵測器的追蹤 ID
有關),不要直接套 1.5 秒門檻。

**下一步**:到達率改用去重後的逐週期通過事件,取代「紅燈排隊成長」推估
(現估 515 vph 對比去重通過量 1149 vph,差 2.2 倍,匝道車流成群到達時
紅燈成長法會低估)。這同時可能解決問題二的相關係數。

### 7.12 通過量/流量:三個量測互相矛盾(未解,不可引用任何一個)

使用者 2026-09-06:「in out 不看,我們看流量」。流量已接上
(`signal_eval.load_flow` 讀 `signal_shadow_log.flow_vpm_1/2`,每 5 秒一筆,
歷史查得到)。但把三個獨立量測擺在一起,它們互相矛盾:

| 量測 | 09-05 17-20 換算 vph | 基礎 |
|------|---------------------|------|
| 系統流量(flow_vpm) | 分相1 1596 / 分相2 1412 | track 消失計一台,60 秒視窗 |
| traffic_events 全部列(舊算法) | 1044 / 1358 | 混了每幀偵測列與別的進場道 |
| traffic_events 只取 EXIT | 291 / 361 | 每車一筆的通過紀錄 |
| 紅燈排隊成長推估的到達率 | 合計 515 | 排隊長度 ÷ 每車 6 m |
| **實測飽和流 × 綠燈佔比 = 容量** | **約 533 / 380** | 飽和流 1184 / 909 vph |

長時間下進場等於出場,所以真實流量**不可能穩定高於容量**。前兩列都高於容量
(流量是容量的 3 倍),不可能成立;後兩列低於容量且彼此量級相符。

已知的兩個偏差方向都來自分析率只有約 0.8 fps:
  · 低估 —— 快車兩幀之間穿過 ROI,track 接不起來就當沒出現過
  · 高估 —— track 斷掉再接會被算成兩台(ID 跳動)

也已排除一個可能:排隊長度換算車數是可信的。有隊伍的樣本(停等 ≥2 台)
實測 每車 6.13 m(NE-2)、6.32 m(WN-2),與落地的 6.0 m 相符。

**尚未確定的是「容量那一側是不是被低估」**:NE-2 有 2 個車道,若
`estimated_queue_length_m` 是單一車道的長度,飽和流與容量都要乘上車道數,
容量會變約 1066 / 380 vph —— 仍低於流量,但差距從 3 倍縮到 1.5 倍。

🛑 **在解決之前,任何「每小時通過車輛數」都不可引用**,包含 §7.9 與
09-04/09-05 每日報告裡的數字。`flow_capacity_check()` 會在流量超過容量時
標記 `over_capacity`,呼叫端必須照實呈現。

**要做的決定性量測**:在停止線相機上,以人工或高幀率計數一段(例如 10 分鐘)
實際通過車輛數,與同期的 flow_vpm、EXIT 計數、容量推估對照。只有這個能定案。

### 7.13 現場實際在跑的是「固定時制 + 時段計畫」,不是動態控制(2026-09-06 實證)

使用者 2026-09-06:「中央只會對固定時制」。用 09-05 全日(06:00–24:00,
分相1 839 段、分相2 838 段綠燈)驗證,結論一致:

**證據一:綠燈長度幾乎不隨車流變化**

| 分相 | 綠燈 vs 起始排隊 | vs 段內最大排隊 | vs 起始流量 |
|------|-----------------|----------------|------------|
| 分相1 | r = 0.019 | r = 0.085 | r = −0.112 |
| 分相2 | r = 0.156 | r = 0.224 | r = −0.073 |

依排隊分四組看,分相1 幾乎是平的(排隊 4.2 m → 綠燈 29.2 秒;排隊 41.7 m →
綠燈 34.8 秒)。真正的感應控制不會長這樣 —— 綠燈長度應該跟需求高度相關。

**證據二:綠燈長度高度集中在少數值(計畫驅動的特徵)**
分相1 最常見的是 24/22/23/21/20 秒,前五個值就佔 50%;分相2 前五個值佔 40%。
連續調整的控制不會這樣堆在幾個整數上。

**證據三:平均綠燈隨時段整段跳動(時段計畫)**
06 時 50/47 秒 → 07–08 時 28/32 → 白天 28–39/30–39 → 19–21 時 37–38/43–45
→ 22–23 時 33–49/68–69。這是時段計畫切換,不是即時反應車流。

**這對整份報告的影響(重要)**
- `signal_shadow_log.control_mode` 全日都是 `external_dynamic` —— 那是**控制器
  回報的模式碼**,不是行為。行為上它是固定時制。以後不可以用這個欄位主張
  「我方是在跟一套動態控制比」。
- §7.8 的配對比較,實際對照組是**固定時制 + 時段計畫**。這與使用者所說
  「OPAC 是最差的控制、沒法驗收」一致 —— benchmark 裡固定時制正是最差的一項
  (十情境平均每車延滯 107.55,離最佳解 +240%)。
- 因此對外的成效主張應該寫成「相對於現行固定時制」,而不是「相對於現行動態控制」。
- 我方抄錄器只看得到控制器發出的訊框(近 6 小時 2441 筆全部 src=controller,
  沒有任何 center 方向),所以「中央送了什麼」我們看不到,以上全部是從
  控制器回報的結果反推。要直接讀到指令,需要把中央→控制器方向也接進抄錄。

### 7.14 綠側價值權重 keep_weight 1.0 → 3.0 上線(2026-09-06,使用者決定)

**改了什麼**:換相門檻由 `綠側價值 + 換相成本` 改為 `綠側價值 × 3.0 + 換相成本`。
成本函數的**結構不變**,只調綠側那一項的權重 —— 這是現場校正係數,不是模型改版。

**為什麼**:綠側價值的算式只看得到「當下排隊 + 換相後等待期間會到達的車」,
看不到綠燈期間持續到達、持續被消化的那一段,也就是綠燈真正的價值被低估,
引擎因此過度願意換相。三條**互相獨立**的線索指向同一個方向:

1. 感應控制(等綠側放完才切)在十情境上贏規則版 13.5%,而且換相次數更少
   (2.33 vs 3.13 次/分)
2. 現場配對的歧異幾乎全是「我方切 / 實際續綠」,反向(我方續 / 實際切)長期為 0
3. 參數搜尋**只用訓練情境**,獨立收斂到 3.0

**驗證方式**:訓練/驗證分離。只在五個訓練情境上搜參數,成績看五個**沒調過**的
驗證情境:

| | 平均每車延滯 | 離最佳解 |
|---|---|---|
| keep_weight 1.0 | 155.67 | +189.6% |
| keep_weight 3.0 | 80.99 | +50.7% |

訓練集變好、驗證集也變好,才算改進。逐情境:尖峰同步 87.99→40.75、
分相2 偏重 41.48→17.40、接近容量 589.95→303.29、突發湧入 41.62→28.30、
長波動 10.77→10.47。

**怎麼落地**:預設值寫在程式裡(`api/routes/signal_shadow.py` 的 `KEEP_WEIGHT`),
不是只設環境變數 —— 這樣它會進版控、會被 review,也不會因為某台機器忘了設而
悄悄跑回舊行為。要臨時回退才用 `SIGNAL_KEEP_WEIGHT=1.0`。
`/api/signal/shadow/plan` 的 constants 會帶 `keep_weight`、`keep_weight_source`、
`keep_weight_since`,決策理由字串也會顯示 `綠側價值 N×3`,看報表的人知道自己在看哪一版。

🛑 **2026-09-06 之前的逐時列與配對數字是 keep_weight=1.0,不可與之後並列。**
這是繼 09-05 22:20 飽和流分母修正之後的第二個參數分界點:
  · 09-05 22:20 前:飽和流分母錯誤,換相成本約 3.3
  · 09-05 22:20 ~ 09-06 上線:飽和流已修,換相成本 6.3~7.3,keep_weight=1.0
  · 09-06 上線後:keep_weight=3.0

**預期方向**:早切次數下降、有車一致率上升、代價秒數下降。若一致率不升反降,
或出現「我方續綠 / 實際換相」的歧異(長期為 0 的那個方向),代表權重調過頭,
用環境變數回退到 1.0 並回報。

