# 延長綠燈（5F1C）上線做法 — PLANNING

**站點**：國道8號新市交流道（東向 0xFFFF）　**撰寫**：2026-09-18
**分相對應**：分相1 = 下匝道（WN，儲車 600 m，主線保護）／分相2 = 上匝道（NE，儲車 210 m）
**規範依據**：16613 K(C)d「輸出時制無固定週期，**僅考慮延長或結束綠燈**」—— 延長是規範要求的能力，不是選配。

---

## 一、為什麼要做

我方目前在現場**只能提早結束**綠燈；延長功能寫好了，但停在影子模式（只算、不下發）。

2026-09-18 的教訓：模擬允許把綠燈延長到 100 秒，調出來的參數（keep_weight=12）上線後，
演算法幾乎不介入（5F1C 從 71 則掉到 5 則），已撤回。改用「只能提早切」重調後，
參數 3~12 的延滯差不到 1% —— **提早切的改善空間已經見底，下一個槓桿就是延長。**

---

## 二、怎麼下（已受控驗證，見 `5F1C延長綠燈_受控驗證紀錄.md`）

| 用途 | 命令 | 意思 |
|---|---|---|
| 提早切（現況） | `5F1C(0,0,0)` | 跳下一步階 |
| **延長** | `5F1C(目前分相, 1, T)` | 把步階1（主綠）**總長**改成 T 秒；控制器把剩餘設為 `T − 已亮` |

09-15 實測：已亮 5.2 秒送 `5F1C(2,1,40)` → 34.9 秒後結束，延長 10 秒，清道（行閃 5 → 黃 3.1 → 全紅 2）正常。

### 送出條件（全部成立才送）

| # | 條件 | 理由 |
|---|---|---|
| 1 | 成本式判「續綠」 | 延長只是續綠的執行方式，不另立決策 |
| 2 | 主綠燈（步階1）、非清道、資料新鮮、時相控制中 | 狀態不確定就不下命令 |
| 3 | 剩餘 ≤ 6 秒才評估 | 快結束才決定，不提前鎖定 |
| 4 | 剩餘 ≥ 4 秒才送 | **黃燈保護**：控制器不保護最短黃燈（09-14、09-15 兩次事件） |
| 5 | 與我方上一則命令 ≥ 5 秒 | 不連送 |
| 6 | 授權 > 8 秒沒續就先補 5F10 | 授權太舊時 5F1C 會被拒（ErrorCode=02，09-17 實測） |

### 延幾秒：四個上限取最小，單次 ≤ 10 秒，< 3 秒不送

| 上限 | 算法 |
|---|---|
| 需求 | 綠側排隊車數 × 飽和車間距 − (剩餘 + 行閃) |
| 最大綠 | 主綠 + 行閃 ≤ 100 秒（控制器硬上限 210） |
| 對向儲車 | 對向排隊 + 對向到達 × (剩餘 + Δ + 清道) ≤ 儲車 × 80%（下匝道 480 m、上匝道 168 m） |
| 週期 | 本相 + 對向最短一輪 ≤ 120 秒 |

延完剩餘變長，等再剩 ≤ 6 秒時會再評估一次 —— 可以接力，但每一步都重查對向。

---

## 三、目前掌握的證據

### 3.0 已經真的下發過一次（09-15 限時實測）

設定檔備份顯示 09-15 曾以 `SIGNAL_EXTEND_GREEN=1`、`SIGNAL_EXTEND_UNTIL=2026-09-15T16:00` 限時開啟：

| 項目 | 結果 |
|---|---|
| 期間 | 13:24:46 ～ 15:48:29 |
| 實際下發 | 78 則 `5F1C(分相,1,T)` |
| 控制器回應 | **ACK 78 / 78**，沒有被拒 |
| 是否生效 | 送出時剩 ≤ 6 秒；送出後到步階1 結束 **中位 10.6 秒**（範圍 8.2～46.5）→ 延長確實發生 |

🛑 這段期間緊接著 16:00 現場斷電施工，之後的號誌資料受污染（見斷電後遺症紀錄），
所以 09-15 只能證明「命令被接受且生效」，**不能拿來評估成效**。
還沒檢查的：這 78 次延長期間的黃燈長度與對向最大排隊（列入 P0-4）。

### 3.1 影子紀錄（09-15 15:49 ～ 09-18 11:07）

- 評估 3,497 次，「想延」385 次（11%）
- 想延秒數：3 秒 20｜4 秒 73｜5 秒 60｜6 秒 44｜7 秒 8｜8 秒 20｜9 秒 24｜**10 秒 136**
- 卡住的上限：需求 3,361、單次上限 136；**對向儲車／週期／最大綠 一次都沒卡到**

依計畫與匝道分開（09-16 起）：

| 計畫 | 時段 | 綠燈 下/上 | 下匝道想延 | 上匝道想延 |
|---|---|---|---|---|
| 35 | 07:00–08:31 早尖峰 | 45 / 25 | **34%**（平均 7.8 s） | 樣本 3 筆 |
| 37 | 16:31–19:00 晚尖峰 | 25 / 40 | **18%**（6.4 s） | 6% |
| 1 | 08:31–16:31 離峰 | 35 / 40 | 14%（6.5 s） | 8% |
| 36 | 夜間 | 35 / 25 | 4% | 6% |

### 3.2 照現場規則的模擬（11 個通過校準的窗，驗證日 09-17）

| 窗 | 只提早切 | 切 + 延長 | 延長效益 |
|---|---|---|---|
| 其餘 9 窗 | — | — | **0%**（延長從未觸發或無差異） |
| 09-14 晚尖峰 | 141.5 s/車 | 20.9 s/車 | −85%（最大排隊 620 → 95 m） |
| 09-11 晚尖峰 | 17.4 s/車 | 12.0 s/車 | −31%（105 → 42 m） |
| 驗證日平均 | 9.73 | 9.73 | 0% |

### 3.3 判讀與保留

- **效益集中在重度壅塞的晚尖峰**（計畫 37，下匝道只給 25 秒），其他時段幾乎沒有差別。
- 🛑 **幅度不可信**：那兩窗連「只提早切」的模擬排隊都到 620 m，現場實測從沒超過約 100 m ——
  模型在重度壅塞時會發散。方向可信，數字不能拿來宣稱。
- 🛑 **影子與模擬矛盾**：影子說早尖峰下匝道 34% 想延，模擬說早上延長沒有效益。
  上線前必須先解釋（見第四節 P0-3）。

---

## 四、分階段做法

### P0　不碰現場（進行中）

| # | 工作 | 狀態 |
|---|---|---|
| P0-1 | 影子紀錄盤點（次數、秒數、卡住的上限） | ✅ 完成（第三節） |
| P0-2 | 照現場規則模擬 | ✅ 完成（第三節） |
| P0-3 | 解釋影子 34% vs 模擬 0% 的矛盾 | ⬜ 未解（見下，15:40 更正） |
| P0-4 | 逐筆檢查「想延」當下對向是否已在排 | ✅ 完成（見下） |
| P0-5 | 運作統計頁加「延長綠燈」卡（現在狀態、試行時段、自動停止、逐筆紀錄與實際結果） | ✅ 完成（da956c8） |

**P0-3 結果（2026-09-18，15:40 更正）**：🛑 早上寫的「線上延長用 1800 vph 預設、量測值沒載入」**是錯的**。那是我另開一支 Python 程序讀模組變數得到的——那支程序沒有執行載入步驟。現場服務實際載入的是`data/signal_saturation.json` 的 **942 / 951 vph**（7 天視窗、10.5 萬筆樣本，15:26 重啟時重量）。因此：飽和流**不是**矛盾的原因（線上用的值比模擬還低，照理更容易判要延）；先前「延長偏保守、對 P1 風險方向安全」的推論一併撤回。**P0-3 仍未解**，P2 前必查。

**P0-4 結果**：「想延」279 筆中，對向排隊 < 儲車 25% 的有 274 筆、25～50% 的 5 筆、**≥ 50% 的 0 筆**——
沒有在對向已經在排的時候還想延。09-15 實測 78 次延長期間，排隊最大：下匝道 80 m（儲車 600）、上匝道 92 m（儲車 210）；
黃燈（當時框完整）中位 3.0 s，< 2.5 s 占 5.7%，與無下發基準（約 7%，框時間誤差）同級。

### P1　限時試行（2026-09-18 使用者核准，當天 16:30–19:00）

**已設定**（`zz-zz-extend-p1.conf`）：`SIGNAL_EXTEND_GREEN=1`、`SINCE=16:30`、`UNTIL=19:00`、`MAX_DELTA=5`。
停止條件由程式自動判（`_ext_watch`），時段外退回影子。

🛑 **黃燈停止條件目前是盲的**：09-15 斷電後控制器短步階回報遺失，09-17、09-18 各約 90 個綠燈段
**沒有一次**收到完整的黃燈框，無法量測黃燈長度。防護改靠**事前閘門**（剩餘 ≥ 4 秒才送；
09-15 框完整時 78 次驗證過黃燈正常），且與每天數十次提早切命令共用同一套閘門。

- **時段**：晚尖峰 16:30–19:00（證據最集中的地方），先試 1 天
- **設定**：`SIGNAL_EXTEND_GREEN=1`、`SIGNAL_EXTEND_UNTIL=<當天 19:00>`（時間到自動回影子模式）、
  `SIGNAL_EXTEND_MAX_DELTA=5`（第一天單次先限 5 秒，減半風險）
- **立即停止條件**（任一成立就回影子模式）：
  1. 任一側排隊 ≥ 儲車 80%（下匝道 480 m／上匝道 168 m）——**程式自動**
  2. 延長後控制器沒有照 T 結束（剩餘與 `T − 已亮` 差 > 2 秒，累計 2 次）——**程式自動**
  3. 延長命令被拒累計 3 次——**程式自動**
  4. 黃燈 < 2.5 秒或黃燈退回綠燈——🛑 **目前無法量測**（見上）
  5. 時相控制保持率 < 95%——試行報告檢查（17:00、19:05 自動產出）

### P2　擴大

- P1 無異常 → 單次上限回 10 秒、時段擴大到全天，觀察一週
- 每天比對：延長次數、每次延長量、對向最大排隊、主線回堵次數、下匝道放行車數

### P3　系統性缺口改計畫（另案，需報備中央）

早尖峰下匝道 34% 想延、平均 7.8 秒，是**穩定的缺口**，這種缺口改時制計畫（5F15）比每週期去延更對症。
但改計畫會讓控制器與官方時制表不一致、中央看得到，而且我方故障時新秒數不會自動回復 ——
要依實測到達率算出兩相都放得完的分配、報備後再寫入。**延長上線與改計畫分開決策。**

---

## 五、回退

| 情況 | 做法 | 生效時間 |
|---|---|---|
| 試行時段結束 | `SIGNAL_EXTEND_UNTIL` 自動回影子模式 | 立即 |
| 觸發停止條件 | `SIGNAL_EXTEND_GREEN=shadow` → 重啟 traffic-api | 約 10 秒 |
| 我方當機 | 不續約 → 授權 1 分鐘內過期 → 控制器回內建時制 | ≤ 1 分鐘（fail-safe） |

延長只改這一次綠燈的步階總長，不寫入時制計畫；授權一過期，控制器就回到原計畫。

---

## 六、參數一覽（現行值）

| 環境變數 | 值 | 說明 |
|---|---|---|
| `SIGNAL_EXTEND_GREEN` | `shadow` | 0 關／1 下發／shadow 只算不送 |
| `SIGNAL_EXTEND_UNTIL` | （空） | 試行截止時刻（ISO） |
| `SIGNAL_EXTEND_TRIGGER_REMAIN` | 6 | 剩 ≤ 這個才評估 |
| `SIGNAL_EXTEND_MIN_REMAIN` | 4 | 剩 < 這個不送（黃燈保護） |
| `SIGNAL_EXTEND_MIN_GAP_SEC` | 5 | 與我方上一則命令的間隔 |
| `SIGNAL_EXTEND_MIN_DELTA` | 3 | 算出來 < 這個不送 |
| `SIGNAL_EXTEND_MAX_DELTA` | 10 | 單次最多延幾秒 |
| `SIGNAL_EXTEND_CYCLE_MAX` | 120 | 週期上限 |
| `SIGNAL_EXTEND_OPP_STORAGE_RATIO` | 0.8 | 對向儲車保護比例 |
| `SIGNAL_TC3_AUTH_REFRESH_BEFORE_SEC` | 8 | 5F1C 前授權超過這個秒數就先補 5F10 |
