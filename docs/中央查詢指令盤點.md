# 中央電腦查詢指令盤點（TC3）

> 資料源：Jetson `data/violations.db` 的 `signal_frames`（抄錄期間 2026-08-28 00:44 ~ 09-03 11:12）。
> 建立：2026-09-03。本檔存開發機，現場不留檔。

## 〇、先講結論與**已知不完整之處**

**直接觀測到中央送出的查詢只有 3 個**，但**證據顯示中央實際問的是這 3 個的兩倍量**，
另一半走的不是我們這條中繼。詳見第二節。

---

## 一、直接側錄到的中央下傳（`src=center`）

只有 8/28 中央連我們 :1001 中繼那段可見。**其後中央改直連控制器，我們只看得到回應。**

| 指令 | 用途 | 規範頁 | 筆數 | 週期 |
|---|---|---|---|---|
| `5F40` | 目前控制策略查詢 | 5-8 | 382 | 60s（338/349 筆） |
| `5F48` | 目前時制計畫查詢 | 5-43 | 379 | 60s |
| `0F41` | 設備通訊重新啟動與檢測（心跳） | 4-21 | 378 | 60s |

三個一組、每分鐘一輪。中央問的就三件事：**現在誰在控制、跑哪個計畫、你還活著嗎。**

> `5F40` 每 60 秒一次 = OPAC 死人開關的心跳。先前推得的
> 「5F10 effectTime 5 分鐘、每 60 秒續約」在這裡對上了。

## 二、🛑 這份側錄只涵蓋一半的查詢

同一段時間內，**控制器的回應是查詢的整整兩倍**：

| 中央送出 | 速率 | 控制器回應 | 速率 |
|---|---|---|---|
| `5F40` | 1.02 筆/分 | `5FC0` | **2.01 筆/分** |
| `5F48` | 1.02 筆/分 | `5FC8` | **2.01 筆/分** |
| `0F41` | 1.02 筆/分 | `0FC1` | **2.01 筆/分** |

回應間隔是乾淨的 **12 秒 / 48 秒交替**（332 次 / 330 次）——
**不是重複記錄，是兩個查詢來源各問一次、相差 12 秒**，其中只有一個走我們中繼。

另外 `5FCC`（時相步階變換控制管理·查詢回報）在同段出現 **2.98 筆/分**，
中央側**一筆對應查詢都沒有** —— 第三種來源或第三種觸發，未確認。

**中繼視窗本身也不完整**：8.73 小時的視窗中
**06:29 → 08:58 整段（2h29m）沒有任何 center frame**，
理論 524 筆只收到 382 筆，實際連續觀測僅 6 小時 15 分。

## 三、我方自己送出的查詢（`src=self`）— 不要誤判成中央行為

8/29 01:39~01:45 的一次 `_run_self_probe`：

| 指令 | 用途 | 筆數 |
|---|---|---|
| `5F45` | 時制計畫資料庫查詢 | 50 |
| `5F44` | 時制計畫基本參數查詢 | 50 |
| `5F40` / `5F48` / `0F41` | 同中央那三個 | 各 2 |

⚠️ 因此 `5FC3` / `5FC4` / `5FC5` / `5FEF` 這些回報**多半是我方觸發的**，
最近一次落在 2026-09-03 10:55~10:56。**判讀中央行為時要先排除。**

## 四、控制器送出的全部碼（`src=controller`）

### 主動回報
| 碼 | 內容 | 筆數 |
|---|---|---|
| `5F03` | 燈態／分相／步階（影子決策吃這個） | 188,821 |
| `0F04` | 設備硬體狀態 | 9,376 |
| `5F0C` | 時相步階變換控制 | 1,140 |
| `5F00` | 目前控制策略 | 507 |
| `5F08` | 現場操作回報 | 10 |

### 查詢回報
| 碼 | 內容 | 筆數 |
|---|---|---|
| `0FC1` | 通訊檢測＋硬體狀態 | 18,696 |
| `5FC0` | 目前控制策略 | 11,475 |
| `5FC8` | 目前時制計畫 | 9,576 |
| `5FCC` | 時相步階變換控制 | 2,774 |
| `5FC5` | 時制計畫資料庫 | 94 |
| `5FEF` | 燈態步階傳輸週期 | 79 |
| `5FC4` | 時制計畫基本參數 | 71 |
| `5FC3` | 時相資料 | 58 |
| `5FC9` | 觸動控制組態 | 14 |
| `5FC6` | 一般日時段型態 | 13 |
| `5FCE` | 特勤路線控制（VIP）參數 | 8 |
| `5FC7` | 特殊日期段時段型態 | 5 |
| `0FC3` | 設備韌體燒錄日期及版本 | 1 |

### ACK
`0F81`（9,564）、`0F80`（5,349）、`0F92`（154，日期時間設定回報）

### 假幀（**不是指令**）
`0008` `0FAA` `5FAA` `AADD` `EE00` `FFFF` 各 1 筆，
全部 `cks_ok=0`，raw 內含巢狀 `AA BB` / `AA DD` / `AA EE` 標頭 ——
**取框失步造成的假邊界**，不要當成未知指令去追。

## 五、✅ 已排除的疑慮：持久化沒有漏資料

8/28~8/31 每天 `5F03` 只有 8,000~9,100 筆（理論 86,400，涵蓋 9~11%），
一度懷疑是佇列 `_frame_q` 滿了靜默丟棄（`except Full: pass`，無計數器）。

查間隔分布後排除 —— **是控制器的燈態播送週期本來就不同**：

| 日期 | 5F03 間隔眾數 |
|---|---|
| 08-28 / 08-30 | 2~5 秒 |
| 09-01 起 | **1 秒**（09-02 全天涵蓋 99%） |

轉折點是 **8/28 16:07 `5FEF`（燈態步階傳輸週期管理）密集出現**，那次改了播送週期。
資料完整，只是採樣率變過。

## 六、要做到真正完整，缺的是這一步

現有資料**只能回答「經我們中繼的那一路問哪些」**，不能回答「中央總共問哪些」。

補齊需要：
1. 把中央連線切回我們 :1001 中繼（**要跟中心端協調，不自行更動**）
2. 連續抄 **24 小時以上**，涵蓋整點／換日／時制切換
3. 確認**兩個查詢來源都走中繼** —— 否則仍只看到一半
4. 順帶釐清 `5FCC` 那 2.98 筆/分 是誰觸發的

建議同時替 `_enqueue_frame` 的 `except _queue.Full: pass` 補一個丟棄計數器 ——
目前佇列滿是靜默的，未來若真的塞爆會無從察覺。
