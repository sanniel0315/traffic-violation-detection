# 交通違規影像分析系統 — 功能整理（簡報用）

> 邊緣 AI 智慧交通系統，運行於 NVIDIA Jetson AGX Orin。
> 本文件逐項對照實際程式碼盤點（file:line 可溯源），並誠實標註成熟度：
> **✅ 生產可展示 ｜ ⚠️ 實驗/部分完成 ｜ ❌ 規劃中未實作**
> 製作日期：2026-06-18

---

## 一、系統總覽

| 項目 | 內容 |
|------|------|
| 定位 | 路口/道路 即時車輛偵測、車牌辨識、違規取締、壅塞分析、停車場車位偵測、號誌 IO 控制 |
| 後端 | FastAPI（:8000）+ SQLAlchemy + SQLite（`data/violations.db`） |
| 前端 | Vue 3 單檔 SPA（`web/index.html`，~18,000 行）+ Element Plus + Chart.js |
| AI 模型 | YOLOv8n（車輛，TensorRT 加速）、YOLO26s-cls（大型車細分）、YOLO 字元/Tesseract（車牌 OCR）、Qwen2-VL-2B（視覺問答）、MobileSAM（車格分割） |
| NVR | Frigate + go2rtc + MQTT |
| 硬體 | Jetson AGX Orin（JetPack 6 / CUDA 12.2 / TensorRT 8.6）、PD3R3 RS-485 IO 模組 |
| 部署 | GitHub Actions self-hosted runner → 自動驗證 + 部署，systemd 多服務隔離 |

**架構特色 — 程序隔離（process isolation）**：核心偵測在 `traffic-api`，但 **OCR、IO、Frigate 各自獨立 systemd 服務**，單一元件 native crash 不拖垮全系統。

---

## 二、子系統功能盤點

### A. 車輛偵測與違規取締　✅

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| YOLOv8 車輛偵測 | 6 類（人/小客車/機車/自行車/大客車/貨車），TensorRT 加速、GPU Lock 防多路 race | ✅ |
| 大型車細分類 | YOLO26s 二階段細分大貨/小貨/大客，對應停等長度估計 | ✅ |
| **停車類違規（9 種）** | 違停、紅線臨停/停車、黃線、行駛/停車人行道、公車招呼站、停斑馬線；bbox 多邊形重疊 + 速度向量判靜止（<8px/s）、60 分 cooldown 去重 | ✅ |
| 逆向行駛 | 60 frame 滑窗統計區域主車流方向，cosine<-0.5 連續 5 frame 判逆向（自動學習，免設定方向） | ✅ |
| 轉彎未禮讓行人 | 斑馬線行人 + 車輛速度>8km/h 或距離<200px 判未停讓 | ✅ |
| 機車未戴安全帽 | YOLOv8 安全帽模型，rider 區連續 4 frame 無帽判違規 | ✅ |
| 4 幀違規拼圖 | 環形緩衝（30 frame）撈 t±2s 共 4 張，拼 2K 複合圖 + 車牌 overlay + 時間 OSD | ✅ |
| 車牌-車輛關聯 | 違規觸發即對車輛裁切跑車牌 OCR，信心分級（≥0.4 寫號碼 / 0.2–0.4 留圖人工複核） | ✅ |
| 雷達-視覺融合 | Hungarian 配對 BEV 投影 vs 雷達，輸出 fused/radar-only/visual-only | ⚠️ 用 MockRadar，實體雷達到貨換驅動即可 |
| 超速偵測 | 類型/罰則已定義，**無實際測速演算法**（需 homography 標定） | ❌ 未實作 |
| 闖紅燈偵測 | 類型已定義，**需號誌燈號整合（CAN/MQTT）** | ❌ 未實作 |

程式碼：`detection/parking_violation.py`、`wrong_way.py`、`pedestrian_yield.py`、`no_helmet.py`、`vehicle_detector.py`、`truck_classifier.py`、`api/routes/stream.py`、`violations.py`

---

### B. 車牌辨識 LPR / OCR　✅

雙層投票決策，務實準確性導向。

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| 車牌邊界偵測 | YOLOv8n（`Charcter-LP`/`plate_yolov8n`）優先，無模型時退啟發式形態學（CLAHE+Sobel+Canny+NMS） | ✅ |
| 整牌 OCR（主路徑） | YOLO 字元偵測 → 同行過濾 → 台灣車牌格式修復（O→0 等） | ✅ |
| 切字 OCR（備援） | 連通域切字 + Tesseract 單字 + Beam search 重組（beam=12） | ✅ |
| OCR 微服務 | **獨立服務 `:8010`**，HTTP 傳圖回 JSON，與主程序解耦可獨立擴展 | ✅ |
| 多幀字元級投票 | 同車（track_id）逐幀累積各字位投票，追蹤結束選最穩號碼 | ✅ |
| 即時 Bucket 投票 | 空間 hash 分桶 + 3.5s TTL，confirm/commit 分層、20s 冷卻防假牌 | ✅ |
| 品質評分門控 | Laplacian 模糊/亮度/對比 3 級分類，低品質跳過、高品質跳備援 | ✅ |
| 即時串流 + 視覺化 | 每攝影機獨立 thread，MJPEG 疊框，DB 持久化（`lpr_records`/快照） | ✅ |

程式碼：`recognition/plate_detector.py`、`plate_recognizer.py`、`api/routes/lpr_stream.py`、`lpr_visual.py`、`services/ocr_service.py`

---

### C. 壅塞偵測　✅

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| 四級等級判定 | 暢通/中等/擁擠/嚴重；混合「佔用率 + 隊列評分」，10-frame 滑窗平滑，門檻 0.2/0.4/0.6 可調 | ✅ |
| 停滯車輛偵測 | 近 4 frame 位移≤18px 判停止，IOU 追蹤、軌跡恢復 | ✅ |
| 隊列長度/持續時間 | 依車種等效長度（小客 5m～大客 12m）+ 間隙累計，狀態機計時 | ✅ |
| 多分區並行分析 | 每車道/區域獨立計算（注：各區平等，**無重要度加權**） | ✅ / ⚠️ 無加權 |
| 資料儲存 + 聚合 | `congestion_samples`（全域+分區）、`congestion_report_aggs`（1m/5m/1h 聚合） | ✅ |
| 完整生命週期 API | 啟停、參數 CRUD、狀態、MJPEG 串流、快照、開機自動恢復、30s watchdog 自愈 | ✅ |
| 主動告警/事件規則 | 僅統計，無主動通知/告警引擎 | ❌ 未實作 |

程式碼：`detection/congestion_detector.py`、`api/routes/congestion.py`、`api/utils/report_aggregation.py`

---

### D. 停車場車位偵測　✅（本系統 AI 亮點）

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| 車位佔用偵測 | ROI 模式（YOLO 車輛中心落格內/IoU 加權，門檻 0.5）+ PKLot 模式 + auto | ✅ |
| 逐格遲滯平滑 | 非對稱遲滯（空→占 2 frame、占→空 3 frame），寧可慢宣告空，去抖動 | ✅ |
| 交叉校正 | 區域內車數 vs 車位佔用數差距大時自動升級 VLM 仲裁 | ✅ |
| 車位編輯器 | 拖框/Grid 透視（4 點 homography）/整排調整/停車場區域/不偵測區/計數線 | ✅ |
| **本地逐格分類器（P4）** | 取代不泛化的巴西 PKLot，用本地資料 fine-tune `local_cls.pt` | ✅ 已上線 |
| **SAM 點選車格** | MobileSAM 懶載入，點停著的車自動框旋轉四邊形（空格仍用 Grid） | ✅ 2026-06-18 上線 |
| 進出計數線 | 跨線計數 + 方向判定 | ✅ |

程式碼：`services/parking_occupancy.py`、`parking_slot_state.py`、`parking_slot_classifier.py`、`parking_sam.py`、`api/routes/parking.py`、`web/parking_editor.html`

---

### E. IO 號誌控制　✅（軟體完成，待現場驗收）

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| PD3R3 RS-485 驅動 | Modbus RTU，3 DI + 3 DO，CRC 校驗、自動重連 | ✅ |
| 獨立 daemon 隔離 | `traffic-io.service`（:8011 localhost），pyserial SEGV 由 systemd 自動拉起、不牽連主 API | ✅ |
| 燈號邏輯 | 紅燈=通訊故障、綠燈=運作正常、白燈=手動/下載中閃爍（2.5Hz） | ✅ |
| DI 觸發 | DI0=遠端設定下載、DI1=Reset（滅燈→重啟 traffic-api）、DI2 保留 | ✅ |
| 網路健康監測 | 5s 輪詢 NTP/網卡 link/IP，任一失敗亮紅燈 | ✅ |
| 設定同步 | DI0 觸發遠端拉 JSON 設定，下載中白燈閃爍 | ✅ |
| Modbus TCP 多站 | tPET/tET 網路模組，多站並行、DO 脈衝（警示燈/蜂鳴） | ✅ |
| 現場電氣/光學驗收 | 實機點燈、按鈕、採購、BIOS RS-485 設定 | ⚠️ 待現場 |

程式碼：`services/pd3r3.py`、`io_module.py`、`io_service.py`、`io_daemon.py`、`modbus_tcp_io.py`、`api/routes/io.py`、`io_tcp.py`

---

### F. VLM 視覺助理　✅

| 功能 | 說明 | 成熟度 |
|------|------|--------|
| 浮動 AI 視覺助理 | 任一攝影機畫面自由問答、多輪對話、可拖曳、圖片放大 | ✅ |
| 語音輸入/輸出 | Web Speech API（STT zh-TW + TTS 朗讀）+ 預設快問 | ✅ |
| 確定性意圖路由 | 幾台車/空位/車種等事實題由偵測資料直接回答，其餘才走 VLM | ✅ |
| 車位佔用仲裁 | Qwen2-VL-2B（eager attn），低信心只做仲裁不覆寫 | ✅ |

程式碼：`services/parking_vlm.py`、`parking_vlm_hook.py`、`web/index.html`（助理元件）

---

### G. 前端與統計報表　✅

15 個主功能頁（Vue3 SPA）。重點：

| 功能 | 說明 |
|------|------|
| 違規件數儀錶板 | 今日 KPI、24h 趨勢、類型/車型/車速分布 |
| **統計分析（三模式趨勢）** | 趨勢選擇 **流量／車速／事件**，圖表隨選切換；多維篩選（點位/攝影機/車道/方向/週期 1s~1d） |
| 即時監控 | 多路實況 + live overlay（框/車牌/車速/違規標記） |
| 違規查詢 / 開單管理 / 累犯查詢 / VD 車道報表 | 複合條件查詢、待簽核、重複車輛排名 |
| 攝影機管理 | 新增/編輯/源測試/上傳影片/效能測試 |
| 硬體監測 / 系統日誌 / 權限管理 / MQTT | 維運面板 |

技術棧：Vue 3 inline template + Element Plus + Chart.js + Leaflet（CDN，免建置）。
程式碼：`web/index.html`、`api/routes/analytics.py`、`cameras.py`、`traffic.py`

---

### H. NVR 整合（Frigate）　✅

- 錄影排程表（每日時段 enable/disable、Motion/Continuous）
- 事件列表 + 回放（`nvr_playback.html` 獨立頁，時間軸拖曳、事件預覽）
- go2rtc 多協議轉碼（RTSP → MJPEG/WebRTC）
- 後端：`api/routes/frigate.py`、`frigate_camera_endpoints.py`

---

## 三、AI / 自動化亮點

### 全自動「自我改進」停車格偵測系統　✅（本週新建）
無人工標註，端到端自動：
```
持續收集多時段畫面 → yolo11m 強模型自動標註（時序穩定+模糊跳過）
   → 訓練逐格分類器 → 品質閘門（防退化模型）→ 通過才部署+重啟,否則回滾
```
- `parking-collector.service`：每 150s 抓幀、滾動保留 800 張
- `parking-retrain.timer`：每日 14:00 自動重訓
- 品質閘門：與強偵測器一致性比對，擋「永遠判空/判占用」退化模型
- 程式碼：`/home/ubuntu/parking_autolabel/`（collector / auto_label / sanity_gate / retrain）

### 開源 AI 導入研究結論
- **離線自動標註**：Grounded-SAM-2 / autodistill（文字提示 + 遮罩，可降模糊跳過）
- **邊緣即時分割**：NanoSAM（NVIDIA TensorRT for Jetson，比 MobileSAM 快 5×）
- **標註平台**：X-AnyLabeling（已裝於開發機，SAM/SAM2 人工抽查）

---

## 四、部署與維運　✅

| 項目 | 內容 |
|------|------|
| 自動部署 | `git push` → GitHub Actions self-hosted runner（Jetson）→ 15 項驗證（GPU/模型/DB/API/前端/Frigate/smoke）→ 自動 `git pull` + 重啟 |
| systemd 服務 | traffic-api / traffic-io / traffic-ocr / traffic-frigate / parking-collector / parking-retrain |
| 時間同步 | systemd-timesyncd（time.google.com，Stratum 1），開機自動同步 + 專屬永久對時 log |
| 日誌持久化 | journald `Storage=persistent`，500M 上限護 eMMC |
| 穩定性 | traffic-api 近 24h 0 重啟、0 SEGV（cap.read SEGV 已修） |

---

## 五、成熟度總表

| 子系統 | 生產可展示 ✅ | 實驗/待現場 ⚠️ | 未實作 ❌ |
|--------|:---:|:---:|:---:|
| 車輛偵測/違規 | 停車9類·逆向·禮讓·安全帽·4幀拼圖·車牌關聯 | 雷達融合(MockRadar) | 超速·闖紅燈 |
| 車牌辨識 LPR | 全管線（雙層投票·微服務） | — | — |
| 壅塞偵測 | 四級·隊列·聚合·自愈 | 分區加權 | 主動告警 |
| 停車場車位 | ROI·遲滯·分類器·SAM·自動標註 | — | — |
| IO 號誌控制 | 全軟體·daemon隔離·燈號·雙協議 | 現場電氣驗收 | — |
| VLM 助理 | 問答·語音·仲裁 | — | — |
| 前端/統計/NVR | 15頁·三模式趨勢·Frigate整合 | 熱點推薦 | — |

---

## 六、技術棧一覽

- **AI**：YOLOv8n / YOLO26s-cls / YOLO 字元 / Tesseract / Qwen2-VL-2B / MobileSAM / yolo11m（離線標註）
- **加速**：TensorRT、CUDA 12.2、GPU Lock 序列化
- **後端**：FastAPI、SQLAlchemy、SQLite、多 systemd 服務隔離
- **前端**：Vue 3 SPA、Element Plus、Chart.js、Leaflet
- **NVR/串流**：Frigate、go2rtc、MQTT、MJPEG overlay
- **硬體 IO**：Modbus RTU（PD3R3 RS-485）+ Modbus TCP（tPET）
- **維運**：GitHub Actions 自動部署、systemd-timesyncd、持久化 journald

---

*本文件由程式碼逐項稽核產出，可作為簡報骨架。各功能均附 file:line 可回溯驗證。*
