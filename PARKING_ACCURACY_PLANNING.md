# 停車位偵測準確性改善計畫 (空位/佔用判定)

> 重點: 提升「**空位 (無車)**」判定準確性。現況靠「YOLO 偵測不到車 = 空位」,
> 是 absence-based,低解析/雨/遮擋下 YOLO 漏抓停著的車 → **有車誤判成空位**
> (最糟: 告訴駕駛有空位,實際已滿)。

---

## 1. 問題分析

### 現況判定 (services/parking_occupancy.py, ROI 模式)
- 每車位: YOLO 車輛**中心落在多邊形** → conf 0.95;或 bbox-polygon **IoU>0.15** → conf=IoU×4。
- `occupied = conf ≥ 0.5`;**空位 = 沒有任何車命中** (absence)。

### 為何「空位」不準 (誤判來源)
| 誤判 | 成因 | 影響 |
|------|------|------|
| **假空位** (有車→判空) | YOLO 漏抓: 低解析(352/640)、下雨低對比、夜間、深色車、車被樹/柱遮、車在格邊緣中心不在多邊形內 | **最嚴重**: 顯示有空位但其實滿 |
| 假佔用 (空→判有車) | 路過車中心剛好進格、陰影/積水/雜物誤判、相鄰車 bbox 壓進來 | 顯示沒空位但其實有 |
| 單張閃爍 | 單幀漏抓就翻面 | 數字跳動、不穩 |

### 根本限制
- **absence-based 判空脆弱**: 「偵測不到」≠「真的沒車」。
- 純整幀 YOLO 看不到「這一格內到底有沒有東西」,只看「有沒有偵測框命中」。
- 低解析 + 天氣 = YOLO recall 下降 → 假空位變多。

---

## 2. 改善策略 (多訊號融合 + 時間平滑 + 逐格分類 + 評測)

核心: **不要只靠 YOLO『偵測不到』判空**,改成多訊號 + 時間穩定 + 直接看每格內容。

### 訊號 (per-slot)
1. **YOLO 命中** (現有): 中心在多邊形 / IoU。→ 偵測到車 = 強佔用訊號。
2. **PKLot 逐格分類器** (關鍵新增): crop 每個車位 ROI → 小 CNN 二分類 (空/有車)。
   **直接看格子內容**,不靠整幀偵測 → 抓得到 YOLO 漏的車、非車佔用(機車/雜物)。
   專案已有 `services/parking_pklot_model.py`,目前只用在「沒標 slot」模式,
   **要拉進 ROI 模式當每格的第二訊號**。
3. **背景參考差異** (選用): 每格存一張「確認空」的參考 patch,現況 patch 跟它做
   SSIM/diff → 很像=高信心空;差很多=有東西。抓 YOLO 漏的車 + 非車物。
4. **VLM 仲裁** (現有,僅 tiebreaker): borderline 才問 Qwen2-VL (低解析下 2B 弱,只當打平用)。

### 融合 + 門檻
- 每格算佔用分數: `score = w1·YOLO + w2·PKLot + w3·refdiff` (+VLM 打平)。
- **遲滯 (hysteresis,非對稱)**: occupied 若 score≥0.6;empty 若 score≤0.3;中間維持原狀。
  **判「空」要更嚴** (因假空位最糟): empty→需更低分 + 更多幀確認。

### 時間平滑 (cheap,效益最大)
- 每格保留最近 N 次評估的 rolling window。
- **狀態改變要連續 K 幀一致才翻**: 空→有車 需 2 幀;**有車→空 需 ≥3 幀**(不對稱,
  讓「宣告空位」更保守)。→ 消除單幀 YOLO 漏抓造成的假空位閃爍。

### 影像品質閘門
- 太暗/太糊/雨太大的 frame → 標記為低品質,**該幀不更新車位狀態** (避免壞 frame 污染)。

---

## 3. 評測 (精確研發,沒這個就是憑感覺)
- **標註資料**: 抓 N 張不同時段/天氣的 frame,人工標每格 空/有車 (ground truth)。
- **分開量**: 「佔用 recall」與「**空位 precision**」(系統說空、實際真空的比例) 分開算。
- **目標 (建議)**:
  - **空位 precision ≥ 95%** (說空就真的空 — 對「可用車位」顯示最關鍵)
  - 佔用 recall ≥ 90% (有車盡量抓到)
  - 狀態穩定: 同一格 1 分鐘內不亂跳
- 用這組 benchmark 調 w1/w2/w3 與門檻,而非憑感覺。

---

## 4. 分階段 (Phasing)

| 階段 | 內容 | 預期效益 |
|------|------|---------|
| **P1 時間遲滯** | 每格 rolling window + 非對稱 hysteresis (有車→空需 3 幀) | **最快、最大**: 大幅減少假空位閃爍 |
| **P2 PKLot 逐格分類融入 ROI** | ROI 模式也跑 PKLot per-slot,與 YOLO 融合 | 抓回 YOLO 漏的車 (低解析/雨/遮擋) |
| **P3 評測 + 調參** | 標註 benchmark,量空位 precision/佔用 recall,調 w/門檻 | 數據化、可迭代 |
| **P4 背景參考差異** (選用) | 每格存空參考 patch + SSIM diff | 抓非車佔用 + 極端漏抓 |
| **P5 品質閘門** | 壞 frame 不更新狀態 | 雨/夜穩定性 |

> P1 先做 (純邏輯、無新模型、效益最大);P2 需 PKLot 模型可用;P3 是長期準確度保證。

---

## 5. 與「無車空場標車位」的關係 (你目前 khh 的情境)
- **標車位 (ROI 定義)** 跟 **佔用偵測準確性** 是兩件事:
  - 標車位: 空場可用 Grid 透視 (不需車);有車可用背景累積。
  - 佔用準確性: 上述 P1-P5,跟有沒有車無關,是判定邏輯。
- 空場標好 ROI 後,本計畫的 P1-P2 才是讓「之後有車/沒車判得準」的關鍵。

---

## 6. 實作落點 (檔案)
- `services/parking_occupancy.py` `evaluate_occupancy` ROI 迴圈: 加 per-slot 訊號融合 + 遲滯。
- 新增 `services/parking_slot_state.py`: per-slot rolling window + hysteresis 狀態機 (per source)。
- `services/parking_pklot_model.py`: 開放 per-slot 分類介面供 ROI 模式呼叫。
- (選用) `services/parking_slot_ref.py`: 每格空參考 patch + SSIM。
- 評測: `tools/parking_eval.py` + `data/parking_labels/*.json` (人工標註)。

---

## 7. 風險 / 備註
- 低解析 (352/640) 是硬限制,P1-P5 是「在限制內盡量準」,不是萬靈丹;高解析來源仍較佳。
- PKLot 模型需確認在目標場景的泛化 (PKLot 是巴西停車場資料集,台灣場景可能要 fine-tune)。
- 遲滯讓狀態「慢一點才翻」是刻意的 trade-off (穩定 vs 即時),參數可調。
- 非對稱遲滯的方向選擇 (寧可慢宣告空) 是因為「假空位」對使用者最傷。
