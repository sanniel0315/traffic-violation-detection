# 即時車流查詢說明

> 給使用端的操作與欄位說明 | 更新日期：2026-09-01

每 5 秒查一次，取得**每一分鐘的即時車流累積**。

### 使用前請先代換兩個值

本文件中的 `{IP}` 與 `{Key}` 為代號，實際使用時請換成系統提供的值：

| 代號      | 代表         | 說明                       |
| --------- | ------------ | -------------------------- |
| `{IP}`  | 系統主機位址 | 由系統管理者提供           |
| `{Key}` | API 金鑰     | 由系統管理者提供，請勿外流 |

---

## 〇、偵測器對照表（道路別名 mapping）

回應裡的 `detector_id` 給的是**偵測器名稱**，不是數字。對照如下：

| `detector_id`（回應值）  | `camera_id` | 車流框（`lanes[].lane_name`） | 進出計數            |
| -------------------------- | ------------- | ------------------------------- | ------------------- |
| `CCTV-N8-E-9-L-NE-1-SIG` | `2`         | 上高速公路前平面道路            | 有               |
| `CCTV-N8-E-9-L-NE-2-SIG` | `3`         | 上匝道前停等區                  | 有               |
|                            |               | 下匝道後平面道路                | 無（未設進出線） |
| `CCTV-N8-E-9-L-WN-1-SIG` | `4` | 高速下匝道 | 有 |
| `CCTV-N8-E-9-L-WN-2-SIG` | `5` | 下匝道停等區 | 有 |

> 車流框的名稱可以在系統中修改，改名後 `lanes[].lane_name` 會跟著變。
> **程式請用 `detector_id` 認偵測器、用 `lane_no` 認車道，不要拿 `lane_name` 當鍵值。**

### 查單一偵測器：兩種寫法都可以

`detector_id` 參數同時接受**數字 `camera_id`** 與**回應裡的名稱**，
所以可以把回應拿到的值直接回填當篩選條件：

```bash
# 這兩行等價
curl -H "X-API-Key: {Key}" "http://{IP}:8000/api/v1/external/realtime?minutes=5&detector_id=3"
curl -H "X-API-Key: {Key}" "http://{IP}:8000/api/v1/external/realtime?minutes=5&detector_id=CCTV-N8-E-9-L-NE-2-SIG"
```

名稱打錯會回 `404` 並附上可用的寫法。
所有吃 `detector_id` 的端點都一樣：`/realtime`、`/vd-report`、`/vd-report/latest`、
`/congestion-report`、`/congestion-report/realtime`、`/congestion-report/latest`。

### `road_name` 目前是「未知」

`road_name` 取自系統中該攝影機的「位置」欄位，目前四台都未填，所以一律回 `"未知"`。
需要正式道路名稱請提供，由系統管理者填入後即會反映在所有報表 API。
**判斷是哪一台請用 `detector_id`，不要用 `road_name`。**

### 車道名稱 `lane_name`

`lanes[]` 每一筆有 `lane_name`，值就是上表的車流框名稱（例如 `"上匝道前停等區"`），
比 `lane_no` 好讀，適合直接顯示在看板上。

---

## 一、怎麼查

### 網址

```
GET http://{IP}:8000/api/v1/external/realtime?mode=minute
```

### postman

![image](https://hackmd.io/_uploads/SkKig3LUMl.png)

### 認證

在 HTTP Header 帶入金鑰：

```
X-API-Key: {Key}  
```

沒帶或帶錯會回 `401`。

### 三種寫法

**curl**

```bash
curl -H "X-API-Key: {Key}" \
  "http://{IP}:8000/api/v1/external/realtime?mode=minute"
```

**Python**

```python
import requests

r = requests.get(
    "http://{IP}:8000/api/v1/external/realtime",
    params={"mode": "minute"},
    headers={"X-API-Key": "{Key}"},
    timeout=10,
)
data = r.json()["data"]

for rec in data["records"]:
    print(rec["detector_id"], rec["total_flow"])
```

**PowerShell**

```powershell
$r = Invoke-RestMethod "http://{IP}:8000/api/v1/external/realtime?mode=minute" `
     -Headers @{ "X-API-Key" = "{Key}" }
$r.data.records | Format-Table detector_id, total_flow, in_flow, out_flow
```

### 線上文件（可直接試打）

```
http://{IP}:8000/api/v1/external/docs?token={Key}
```

---

## 二、資料是怎麼累積的

`mode=minute` 的意思是：**從當前整分開始，累積到你查詢的那一刻**。

```
09:26:20 查詢 → 涵蓋 09:26:00 ~ 09:26:20
09:26:40 查詢 → 涵蓋 09:26:00 ~ 09:26:40    起點不變，數字只增不減
09:26:58 查詢 → 涵蓋 09:26:00 ~ 09:26:58
09:27:12 查詢 → 涵蓋 09:27:00 ~ 09:27:12    跨分，自動歸零重算
```

**分鐘內任何時間查都可以**，不必挑時間點。

回應中：

| 欄位             | 意思                          |
| ---------------- | ----------------------------- |
| `period.start` | 該分鐘的起點（一定是`:00`） |
| `period.end`   | 你查詢的時刻                  |
| `elapsed_sec`  | 這一分鐘已經過幾秒            |

> **分鐘結束時的累積值，就是那一分鐘的最終統計值。**
> 若只需要每分鐘的最終數字（例如存進資料庫），也可改用
> `GET /api/v1/external/vd-report/latest?minutes=5&interval=1m`，
> 那是已結束、不重疊的固定分鐘，適合存檔。

---

## 三、回應內容

```json
{
  "status": "success",
  "data": {
    "mode": "minute",
    "elapsed_sec": 16,
    "period": { "start": "2026-08-10T09:26:00+08:00",
                "end":   "2026-08-10T09:26:16+08:00" },
    "stats": { ... 統計摘要 ... },
    "records": [ ... 每台偵測器一筆 ... ]
  }
}
```

### `records[]` — 每台偵測器一筆

```json
{
  "detector_id": "CCTV-N8-E-9-L-NE-2-SIG",
  "road_name": "未知",
  "status": "active",
  "time_start": "2026-08-15T09:26:00+08:00",
  "time_end":   "2026-08-15T09:26:16+08:00",
  "direction": "INOUT",
  "direction_label": "進出",
  "total_flow": 17,
  "flow_per_hour": 3825.0,
  "small_vehicle_flow": 14,
  "large_vehicle_flow": 3,
  "avg_speed_kmh": null,
  "avg_occupancy_pct": 29.6,
  "avg_queue_length_m": 0.8,
  "max_queue_length_m": 11.5,
  "queue_duration_sec": 3.0,
  "max_queue_duration_sec": 1.0,
  "lane_count": 2,
  "lanes": [ { "lane_no": 1, "lane_name": "上匝道前停等區",  "flow": 12, "...": "..." },
             { "lane_no": 2, "lane_name": "下匝道後平面道路", "flow": 5,  "...": "..." } ],
  "direction_counts": { "IN": 10, "EXIT": 7, "INOUT": 6 },
  "in_flow": 10,
  "out_flow": 7
}
```

| 欄位                                                | 說明                                                                          |
| --------------------------------------------------- | ----------------------------------------------------------------------------- |
| `detector_id`                                     | 偵測器名稱                                                                    |
| `road_name`                                       | 道路名稱                                                                      |
| `status`                                          | `active` 正常運作／`disabled` 該偵測器停用                                |
| `time_start` / `time_end`                       | 本筆涵蓋的時間範圍                                                            |
| `direction` / `direction_label`                 | 行車方向代碼／中文                                                            |
| **`total_flow`**                            | **本時段通過的車輛數**                                                  |
| `flow_per_hour`                                   | 換算成每小時車流率（由`elapsed_sec` 外推，判讀請見第四節第 6 點）           |
| `small_vehicle_flow`                              | 小型車（小客車、小貨車、機車…）                                              |
| `large_vehicle_flow`                              | 大型車（大貨車、大客車、聯結車）                                              |
| `avg_speed_kmh`                                   | 平均車速                                                                      |
| `avg_occupancy_pct`                               | 平均佔用率（%）——**VD 定義**，與壅塞 API 的同名欄位算法不同，見第八節 |
| `avg_queue_length_m` / `max_queue_length_m`     | 平均／最大排隊長度（公尺）                                                    |
| `queue_duration_sec` / `max_queue_duration_sec` | 排隊累計／單次最長時間（秒）                                                  |
| `lane_count` / `lanes[]`                        | 車道數與各車道明細                                                            |
| `lanes[].lane_no` / `lanes[].lane_name`         | 車道編號／**車道名稱**（即車流框名稱，見第〇節）                        |
| **`in_flow` / `out_flow`**                | **進場／出場車輛數**（逐框設定，見第四節第 2 點）                       |

### `stats` — 統計摘要

| 欄位               | 說明                          |
| ------------------ | ----------------------------- |
| `record_count`   | `records` 的筆數            |
| `detector_count` | 偵測器數量                    |
| `bucket_count`   | 時間段數量（本模式恆為`1`） |
| `overall`        | 全部偵測器加總                |
| `by_detector[]`  | 各偵測器小計                  |
| `peak_bucket`    | 本次車流最高的偵測器          |

> `record_count = detector_count × bucket_count`。
> 本模式只有一個時間段，所以 `records` 與 `by_detector` 數字相同，
> 取任一個即可；`records` 的欄位比較完整。

---

## 四、判讀時要注意的七件事

### 1. `total_flow` 不要加上 `in_flow` / `out_flow`

三者是**各自獨立**的計數，相加會重複計算。

| 事件           | 計入`total_flow` | 計入`in_flow`／`out_flow` |
| -------------- | ------------------ | ----------------------------- |
| 一般通過偵測區 | 計入 | 不計入 |
| 駛入進出框     | 不計入 | 計入 `in_flow` |
| 駛離進出框     | 不計入 | 計入 `out_flow` |

`total_flow` 本身已是完整車流量。

### 2. 進出計數是「逐框」設定的，沒設進出線的框只有總流量

進出計數不是整台攝影機的開關，而是**每個車流框各自**設定：在框的邊界上標出
哪一條是「進」、哪一條是「出」，車輛跨越才計數。**沒標的方向一律不計**。

| 框的設定       | `in_flow` | `out_flow` | `total_flow` |
| -------------- | ----------- | ------------ | -------------- |
| 進線、出線都標 | 有值     | 有值      | 照算        |
| 只標進線       | 有值     | 恆為`0`    | 照算        |
| 兩條都沒標     | 恆為`0`   | 恆為`0`    | 照算        |

哪一台有進出計數請對照第〇節。目前只有 `CCTV-N8-E-9-L-NE-2-SIG` 的
**下匝道後平面道路**沒設進出線。實測：

```
下匝道後平面道路   進 0 / 出 0 / 一般流量 266
```

`total_flow` 完全正常，進出恆為 0 —— 這是設定造成的，不是故障。

> **`in_flow: 0` 不代表沒有車進場**，也可能是那個框根本沒做進出計數。
> 要分辨請對照第〇節的表，或看同一時段的 `total_flow` 是否有值。

### 3. `null` 代表「沒有量到」，不是 0

- `avg_speed_kmh: null` → 該偵測器**未設定測速**
- `avg_speed_kmh: 0` → 車速真的量到 0（車輛靜止）

這兩者意義完全不同，請勿把 `null` 當成 0 處理。排隊長度等欄位同理。

### 4. `status: disabled` 的偵測器數值恆為 0

該偵測器目前停用中，`total_flow: 0` 不代表沒有車。
偵測器清單固定，不會因為停用就從回應中消失。

### 5. 短時間內的進出量可能不對稱

`in_flow` 與 `out_flow` 在短時間內不一定相等 —— 車輛可能在上一分鐘進場、
這一分鐘才出場，取樣區間的頭尾一定會切到正在框內的車。時間拉長就會趨於平衡。

現場實測（連續 92 分鐘，四台同時）：

| 偵測器                     | 車流框               | `in_flow`     | `out_flow`    | 差異            |
| -------------------------- | -------------------- | --------------- | --------------- | --------------- |
| `CCTV-N8-E-9-L-NE-1-SIG` | 上高速公路前平面道路 | 150             | 147             | −2%            |
| `CCTV-N8-E-9-L-NE-2-SIG` | 上匝道前停等區       | 212             | 208             | −2%            |
| `CCTV-N8-E-9-L-WN-1-SIG` | 高速下匝道           | 359             | 355             | −1%            |
| `CCTV-N8-E-9-L-WN-2-SIG` | 下匝道停等區         | 479             | 467             | −3%            |
| **合計**             |                      | **1200** | **1177** | **−2%** |

**取一分鐘看差異可能到數成，取十分鐘以上就會收斂到數個百分比。**
要對帳請用較長的區間，不要拿單一分鐘的進出差當異常判斷。

### 5-1. `in_flow` / `out_flow` 和 `total_flow` 是不同的量測，不可互相驗算

- `total_flow`：車輛**通過偵測區**的計數，同一台車在區內滯留 30 秒內只算一次
- `in_flow` / `out_flow`：車輛**跨越框邊界**的計數，進一次算一次、出一次算一次

兩者定義不同，比值取決於框的大小與位置 —— 同一時段實測四個框分別是
`0.63`、`0.93`、`1.20`、`1.26` 倍，**沒有固定關係**。

> `in_flow` 比 `total_flow` 大或小都是正常的，不代表任何一邊有問題。
> **不要拿它們互相驗算，也不要相加。**

### 6. 分鐘剛開始時 `flow_per_hour` 會劇烈跳動

`flow_per_hour` 是用 `elapsed_sec` 外推的（`total_flow × 3600 ÷ elapsed_sec`）。
分鐘剛開始時 `elapsed_sec` 很小，一台車就會讓數值跳到很大：

| `elapsed_sec` | `total_flow` | `flow_per_hour` | 說明                          |
| --------------- | -------------- | ----------------- | ----------------------------- |
| 3               | 1              | 1200.0            | 3 秒過 1 台就外推成 1200 輛/小時 |
| 3               | 0              | 0.0               | 3 秒沒車就是 0                |
| 36              | 5              | 500.0             | 時間夠長，數值才穩定          |

要看穩定的車流率，請等 `elapsed_sec` 大於 30 秒，或改用第六、七節那兩支
（桶是完整的一分鐘，沒有外推問題）。

---

## 五、常見問題

**Q：每 5 秒查一次，數字會重複計算嗎？**
不會。同一分鐘內的十二次查詢是「同一分鐘的累積進度」，不是十二段各自獨立的資料。
若要存檔，每分鐘只取最後一次（或改用 `vd-report/latest`）。

**Q：可以指定只查某一台嗎？**
可以，加上 `detector_id`：

```
/api/v1/external/realtime?mode=minute&detector_id=2
```

**Q：查詢頻率有上限嗎？**
每分鐘 120 次。5 秒查一次等於每分鐘 12 次，遠低於上限。

**Q：回應多快？**
實測 12～92 毫秒（平均約 27 毫秒）。以 5 秒一次的節奏連續量測，佔用約 0.5%。

**Q：時間是什麼時區？**
回應中的時間皆為台北時間（`+08:00`）。

**Q：想要每分鐘的最終統計值，不要中途進度？**
用 `/vd-report/latest`，詳見第六節。

---

## 六、每分鐘的最終統計：`/vd-report/latest`

前面那支 `/realtime` 給的是「本分鐘累積到現在」的進度值，同一分鐘內會一直變。
**如果你要的是每分鐘的最終數字（例如存進資料庫）**，用這一支。

### 網址

```
GET http://{IP}:8000/api/v1/external/vd-report/latest?minutes=5&interval=1m
X-API-Key: {Key}
```

| 參數                | 必填 | 預設     | 說明                                                                   |
| ------------------- | ---- | -------- | ---------------------------------------------------------------------- |
| `minutes`         | 否   | `5`    | 回傳最近幾個**已結束**的桶（`interval=1m` 時即分鐘數），1～360 |
| `interval`        | 否   | `1m`   | 桶大小：`1m` / `5m` / `1h`                                       |
| `detector_id`     | 否   | 全部     | 只查某一台                                                             |
| `include_records` | 否   | `true` | 設`false` 只回統計摘要，不回逐桶明細                                 |

**不用自己算時間**，也不用處理時區 —— 給 `minutes` 就好。

### 跟 `/realtime` 的差別

|          | `/realtime?mode=minute`              | `/vd-report/latest`                  |
| -------- | -------------------------------------- | -------------------------------------- |
| 給的是   | 本分鐘累積**進度**，同分鐘內會變 | 已結束分鐘的**最終值**，不會再變 |
| 資料來源 | 原始表，**無延遲**               | 聚合表，**落後約 15 秒**         |
| 適合     | 即時看板、每 5 秒輪詢                  | 存資料庫、對帳、報表                   |
| 時間     | 一定是當下這一分鐘                     | 最近 N 個完整分鐘                      |

###  回傳的時間區間是浮動的，不是固定值

`period.start` / `period.end` **有對齊整分**（`interval=1m` 時秒數一定是 `:00`），
但**每次查到的區間不一樣**，因為終點是這樣決定的：

```
period.end = min( 現在時刻往下對齊到整分 , 聚合已完成到哪一分鐘 )
```

聚合背景作業每 20 秒跑一次、桶關閉後約 15 秒才寫入，所以終點會被聚合進度往回拉。
實際會看到這種情形：

```
10:39:05 查詢 → period 10:33:00 ~ 10:38:00     （10:38 桶還沒聚合完）
10:39:20 查詢 → period 10:34:00 ~ 10:39:00     （聚合追上了，往前跳一分鐘）
10:39:40 查詢 → period 10:34:00 ~ 10:39:00     （同一個區間，資料一模一樣）
```

**所以不要假設「每查一次就會多一分鐘新資料」** —— 有時候兩次查詢會拿到完全相同的內容。

### 怎麼判斷背景作業是否正常

回應中的 `aggregated_through` 就是「資料已經聚合到哪個時刻」：

```json
{
  "data": {
    "interval": "1m",
    "period": { "start": "...T10:34:00+08:00", "end": "...T10:39:00+08:00" },
    "aggregated_through": "2026-08-11T10:39:00+08:00",
    "stats": { ... },
    "records": [ ... ]
  }
}
```

正常會落後現在時刻**一個聚合週期以內（約 1 分鐘）**。
若落後超過 5 分鐘，代表背景聚合有異常，可據此告警。

> `records[]` 的欄位與 `/realtime` **完全相同**，只是 `time_start` / `time_end`
> 是固定不重疊的整分區間。判讀注意事項第 1～5 點同樣適用
> （第 6 點的 `flow_per_hour` 外推問題在這支不存在 —— 桶是完整的一分鐘）。

---

## 七、逐分鐘查詢（保證不缺漏）：`/realtime?minutes=N`

第六節那支 `/vd-report/latest` 讀的是**聚合表**。聚合表有已知缺口，
**會少報**。要每分鐘的完整資料又不能漏，用這一支 —— 同樣是逐分鐘的最終值，
但直接讀原始表，沒有聚合延遲、也沒有缺口。

### 網址

```
GET http://{IP}:8000/api/v1/external/realtime?minutes=5
X-API-Key: {Key}
```

| 參數            | 必填 | 預設  | 說明                                                                                |
| --------------- | ---- | ----- | ----------------------------------------------------------------------------------- |
| `minutes`     | 否   | `0` | 回傳最近 N 個**已結束**的整分鐘，1～180。`0` = 不啟用，維持原本單一視窗行為 |
| `detector_id` | 否   | 全部  | 只查某一台；**數字 `camera_id` 或回應裡的名稱都可以**（見第〇節）           |

不帶 `minutes` 時完全等同舊行為，既有程式不受影響。

### 這一支同時包含車流、進出、佔有率

一次查就拿得到三類數字，不必再打第二支 API：

| 類別             | 欄位                                                                              |
| ---------------- | --------------------------------------------------------------------------------- |
| 車流             | `total_flow`、`small_vehicle_flow`、`large_vehicle_flow`、`flow_per_hour` |
| **進出**   | `in_flow`、`out_flow`、`direction_counts`                                   |
| **佔有率** | `avg_occupancy_pct`（**VD 定義**）                                        |
| 排隊／車速       | `avg_queue_length_m`、`max_queue_length_m`、`avg_speed_kmh`                 |
| 車道明細         | `lanes[]`（含 `lane_name`）                                                   |

`direction_counts` 是該分鐘各方向的原始事件數，例如：

```json
"direction_counts": { "IN": 10, "EXIT": 7, "INOUT": 6 }
```

- `IN` / `EXIT` → 就是 `in_flow` / `out_flow` 的來源
- `INOUT` → 一般通過事件，計入 `total_flow`

> 這裡的 `avg_occupancy_pct` 是 **VD 佔有率**。
> 要壅塞演算法那一版（含最大值與原始面積佔有率）請用第八節的
> `/congestion-report/realtime` —— 欄位同名但算法不同，數字不會一致。

實測輸出（四台 × 3 分鐘）：

```
整體  流量=123  進=84  出=87  佔有率=19.4%
```

### 「不缺漏」是三層保證

| 層級   | 保證                                                   |
| ------ | ------------------------------------------------------ |
| 分鐘   | 逐分鐘產生，不是靠資料反推 —— 中間**不會跳號** |
| 偵測器 | 該分鐘沒車的偵測器一樣回一筆，`total_flow: 0`        |
| 車道   | 設定幾條就回幾條，值`0` 而非空陣列                   |

所以 `records` 筆數恆為 **`minutes × 偵測器數`**。四台查 5 分鐘就是 20 筆，
少一筆就代表有問題，可以直接拿這個數字做檢核。

### 跟前面兩支的差別

|          | `/realtime?mode=minute` | `/realtime?minutes=N`       | `/vd-report/latest`         |
| -------- | ------------------------- | ----------------------------- | ----------------------------- |
| 給的是   | 本分鐘累積**進度**  | 最近 N 個**已結束**分鐘 | 最近 N 個**已結束**分鐘 |
| 資料來源 | 原始表                    | **原始表**              | 聚合表                        |
| 延遲     | 無                        | **無**                  | 約 15 秒                      |
| 缺口     | 無                        | **無**                  |  聚合表   |
| 適合     | 即時看板                  | **存資料庫（建議）**    | 存資料庫（舊做法）            |

### 怎麼存

以 **`detector_id` + `time_start`** 當唯一鍵 upsert，跟第六節做法相同。
每分鐘輪詢一次、`minutes` 給 3～5 即可，重複的直接覆蓋。

### 範例

```bash
curl -H "X-API-Key: {Key}" \
  "http://{IP}:8000/api/v1/external/realtime?minutes=5"
```

```python
r = requests.get(
    "http://{IP}:8000/api/v1/external/realtime",
    params={"minutes": 5},
    headers={"X-API-Key": "{Key}"}, timeout=15,
)
data = r.json()["data"]
assert len(data["records"]) == data["minutes"] * data["stats"]["detector_count"]
for rec in data["records"]:
    upsert(rec["detector_id"], rec["time_start"], rec["total_flow"])
```

---

## 八、即時壅塞佔有率：`/congestion-report/realtime`

壅塞的即時查詢。同樣是逐分鐘、每分鐘每台都有、直接讀原始表。

### 網址

```
GET http://{IP}:8000/api/v1/external/congestion-report/realtime?minutes=5
X-API-Key: {Key}
```

| 參數              | 必填 | 預設  | 說明                                                                      |
| ----------------- | ---- | ----- | ------------------------------------------------------------------------- |
| `minutes`       | 否   | `5` | 回傳最近 N 個**已結束**的整分鐘，1～180                             |
| `detector_id`   | 否   | 全部  | 只查某一台；**數字 `camera_id` 或回應裡的名稱都可以**（見第〇節） |
| `include_lanes` | 否   | `0` | `0` 只回整體；`1` 連車道層一起回                                      |

> 金鑰需要 `congestion_report` 權限（與 `/congestion-report` 相同）。

### 佔有率有三個

| 欄位                      | 說明                                               |
| ------------------------- | -------------------------------------------------- |
| `avg_occupancy_pct`     | 該分鐘**平均**佔有率                         |
| `max_occupancy_pct`     | 該分鐘**最大瞬時**佔有率                     |
| `avg_raw_occupancy_pct` | 未經 queue／density 加權的**原始面積**佔有率 |

單位都是百分比（`0`～`100`），不是 `0`～`1` 小數。

### 其他欄位

| 欄位                                            | 說明                       |
| ----------------------------------------------- | -------------------------- |
| `detector_id` / `camera_name`               | 偵測器                     |
| `time_start` / `time_end`                   | 該分鐘的區間               |
| `zone_name` / `lane_no` / `direction`     | 區域、車道、方向           |
| `avg_vehicle_count`                           | 平均車輛數                 |
| `avg_stopped_vehicle_count`                   | 平均靜止車輛數             |
| `avg_queue_length_m` / `max_queue_length_m` | 平均／最大排隊長度（公尺） |
| `queue_active_duration_sec`                   | 該分鐘排隊狀態累計秒數     |
| `sample_count`                                | 該分鐘的取樣筆數           |

### `sample_count: 0` 的意思

該分鐘**沒有收到取樣**，不是「沒有壅塞」。

沒有取樣的偵測器一樣會補一筆（各項為 `0`），這樣呼叫端才分得出
「那一分鐘暢通」和「那一分鐘沒資料」。判斷請看 `sample_count`：

| 情況                                           | 意思                   |
| ---------------------------------------------- | ---------------------- |
| `sample_count > 0`、`avg_occupancy_pct: 0` | 有量到，該分鐘確實暢通 |
| `sample_count: 0`                            | 沒量到，數值不可採信   |

### 兩種「佔有率」定義不同，不可混用

| 來源                            | 欄位                  | 定義                                                               | 額外欄位                                                               |
| ------------------------------- | --------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| `/realtime`、`/vd-report`   | `avg_occupancy_pct` | **VD**：車輛偵測事件記錄的佔用率                             | 只有平均值                                                             |
| `/congestion-report/realtime` | `avg_occupancy_pct` | **壅塞**：ROI 面積被車輛覆蓋的比例，再經 queue／density 加權 | 另有`max_occupancy_pct`、`avg_raw_occupancy_pct`、`sample_count` |

欄位名稱一樣但**演算法不同，數字不會一致**。
實測同一分鐘同一台：VD 給 `51.0`、壅塞給 `55.0`。

**怎麼選：**

| 需求                           | 用哪一支                                               |
| ------------------------------ | ------------------------------------------------------ |
| 車流、進出、佔有率一次拿到     | `/realtime?minutes=N`（第七節）                      |
| 只要佔有率、但要最大值與原始值 | `/congestion-report/realtime`（本節）                |
| 要分辨「暢通」與「沒資料」     | `/congestion-report/realtime`（看 `sample_count`） |

### 範例

```bash
curl -H "X-API-Key: {Key}" \
  "http://{IP}:8000/api/v1/external/congestion-report/realtime?minutes=5"
```

實測輸出（4 台 × 5 分鐘 = 20 筆）：

```
分鐘         NE-1   NE-2   WN-1   WN-2      ← avg_occupancy_pct (%)
16:07:00      0.0    0.0   58.8   57.3
16:08:00      0.0   13.0   75.1   45.9
16:09:00     19.9   55.0   40.4   54.6
16:10:00      0.0   52.4   41.9   45.0
16:11:00     10.2   14.7   83.1   66.4
```

---

## 九、電子鎖門狀態：`/door-status`

機箱電子鎖的門狀態。**只要有一道門是開的就回「開門」，兩道門都關上才回「關上」。**

### 網址

```
GET http://{IP}:8000/api/v1/external/door-status
X-API-Key: {Key}
```

沒有參數。金鑰需要 `door_status` 權限。

### 判定規則

| 情況 | `state` | `label` |
| ---- | ------- | ------- |
| 任何一道門是開的 | `open` | 開門 |
| 兩道門都確認關上 | `closed` | 關上 |
| 有鎖離線讀不到，無法確認全關 | `unknown` | 狀態不明 |

**「開門」只要一道成立就成立**，不必看另一道。實際事件回放：

```
01:12:38  後門開  → 開門 (開1/關1)
01:46:11  前門開  → 開門 (開2/關0)
06:27:39  後門關  → 開門 (開1/關1)    ← 只關一道，仍是開門
06:28:58  前門關  → 關上 (開0/關2)    ← 兩道都關才變關上
```

### 回應

```json
{
  "status": "success",
  "data": {
    "state": "open",
    "label": "開門",
    "enabled": true,
    "door_count": 2,
    "open_count": 1,
    "closed_count": 1,
    "unknown_count": 0,
    "doors": [
      { "addr": 2, "name": "後門", "online": true, "state": "open",   "label": "開門" },
      { "addr": 3, "name": "前門", "online": true, "state": "closed", "label": "關上" }
    ]
  }
}
```

| 欄位 | 說明 |
| ---- | ---- |
| **`state`** / **`label`** | **聚合後的門狀態。只要顯示一個狀態，看這兩個欄位就好** |
| `enabled` | 電子鎖功能是否啟用；未啟用時 `state` 一律 `unknown` |
| `door_count` | 門的總數 |
| `open_count` / `closed_count` / `unknown_count` | 各狀態的門數 |
| `doors[]` | 每一道門的明細，需要細分是哪一道門時用 |
| `doors[].addr` / `name` | 鎖位址／門名稱（例如 `2` = 後門、`3` = 前門） |
| `doors[].online` | 該鎖目前是否讀得到 |
| `doors[].state` / `label` | 該道門各自的狀態 |

### 判讀時要注意的兩件事

#### 1. `unknown` 不代表門是關的

規則是「兩個門都關上才顯示關上」，鎖讀不到時**無法確認**那一道是關的，
所以回 `unknown` 而不是 `closed`。**請當異常處理，不要當成關上。**

離線的鎖連上一次讀到的舊值也不採用 —— 那是過期的值，不是現況。

> 但如果另一道門確定是開的，即使有鎖離線，仍然回 `open`。

#### 2. 只吃 `X-API-Key` header

`?token=` 與 `Authorization: Bearer` 都會回 `401`。
這與本文件其他端點行為一致。

### 三種寫法

**curl**

```bash
curl -H "X-API-Key: {Key}"   "http://{IP}:8000/api/v1/external/door-status"
```

**Python**

```python
import requests

r = requests.get(
    "http://{IP}:8000/api/v1/external/door-status",
    headers={"X-API-Key": "{Key}"},
    timeout=10,
)
d = r.json()["data"]

if d["state"] == "open":
    print("開門")
elif d["state"] == "closed":
    print("關上")
else:
    print("狀態不明 — 有鎖讀不到，請檢查", 
          [x["name"] for x in d["doors"] if not x["online"]])
```

**PowerShell**

```powershell
$r = Invoke-RestMethod "http://{IP}:8000/api/v1/external/door-status" `
     -Headers @{ "X-API-Key" = "{Key}" }
$r.data.label
$r.data.doors | Format-Table addr, name, online, label
```

---

## 十、錯誤回應

| HTTP    | 意思             | 處理方式                                     |
| ------- | ---------------- | -------------------------------------------- |
| `401` | 金鑰缺少或無效   | 檢查`X-API-Key`                            |
| `403` | 金鑰無此項目權限 | 聯繫系統管理者                               |
| `404` | `detector_id` 找不到 | 檢查名稱拼寫，或改用數字 `camera_id`（見第〇節） |
| `422` | 參數格式錯誤     | 檢查`mode` 只能是 `minute` 或 `window` |
| `429` | 超過查詢頻率上限 | 降低查詢頻率                                 |
