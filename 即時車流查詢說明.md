# 即時車流查詢說明

> 給使用端的操作與欄位說明 | 更新日期：2026-08-10

每 20 秒查一次，取得**每一分鐘的即時車流累積**。

| | |
|---|---|
| 主機 | `100.92.17.87:8000`（經 Tailscale） |
| 金鑰 | `tvd_hwacom_traffic_2026` |
| 偵測器 | 台62基隆段隧道口、62_1、國8、62 交通流量 |

> 現場正式網路的網卡尚未接上，上線後主機位址會改成客戶連得到的位址，金鑰不變。

---

## 一、怎麼查

### 網址

```
GET http://100.92.17.87:8000/api/v1/external/realtime?mode=minute
```

### 認證

在 HTTP Header 帶入金鑰：

```
X-API-Key: tvd_hwacom_traffic_2026
```

沒帶或帶錯會回 `401`。

### 三種寫法

**curl**
```bash
curl -H "X-API-Key: tvd_hwacom_traffic_2026" \
  "http://100.92.17.87:8000/api/v1/external/realtime?mode=minute"
```

**Python**
```python
import requests

r = requests.get(
    "http://100.92.17.87:8000/api/v1/external/realtime",
    params={"mode": "minute"},
    headers={"X-API-Key": "tvd_hwacom_traffic_2026"},
    timeout=10,
)
data = r.json()["data"]

for rec in data["records"]:
    print(rec["detector_id"], rec["total_flow"])
```

**PowerShell**
```powershell
$r = Invoke-RestMethod "http://100.92.17.87:8000/api/v1/external/realtime?mode=minute" `
     -Headers @{ "X-API-Key" = "tvd_hwacom_traffic_2026" }
$r.data.records | Format-Table detector_id, total_flow, in_flow, out_flow
```

### 線上文件（可直接試打）

```
http://100.92.17.87:8000/api/v1/external/docs?token=tvd_hwacom_traffic_2026
```

---

## 二、資料是怎麼累積的

`mode=minute` 的意思是：**從當前整分開始，累積到你查詢的那一刻**。

```
09:26:20 查詢 → 涵蓋 09:26:00 ~ 09:26:20
09:26:40 查詢 → 涵蓋 09:26:00 ~ 09:26:40    起點不變，數字只增不減
09:26:58 查詢 → 涵蓋 09:26:00 ~ 09:26:58
09:27:12 查詢 → 涵蓋 09:27:00 ~ 09:27:12    跨分，自動歸零重算
```

**分鐘內任何時間查都可以**，不必挑時間點。

回應中：

| 欄位 | 意思 |
|---|---|
| `period.start` | 該分鐘的起點（一定是 `:00`） |
| `period.end` | 你查詢的時刻 |
| `elapsed_sec` | 這一分鐘已經過幾秒 |

> **分鐘結束時的累積值，就是那一分鐘的最終統計值。**
> 若只需要每分鐘的最終數字（例如存進資料庫），也可改用
> `GET /api/v1/external/vd-report/latest?minutes=5&interval=1m`，
> 那是已結束、不重疊的固定分鐘，適合存檔。

---

## 三、回應內容

```json
{
  "status": "success",
  "data": {
    "mode": "minute",
    "elapsed_sec": 16,
    "period": { "start": "2026-08-10T09:26:00+08:00",
                "end":   "2026-08-10T09:26:16+08:00" },
    "stats": { ... 統計摘要 ... },
    "records": [ ... 每台偵測器一筆 ... ]
  }
}
```

### `records[]` — 每台偵測器一筆

```json
{
  "detector_id": "台62基隆段隧道口",
  "road_name": "台62線基隆段",
  "status": "active",
  "time_start": "2026-08-10T09:26:00+08:00",
  "time_end":   "2026-08-10T09:26:16+08:00",
  "direction": "straight",
  "direction_label": "直行",
  "total_flow": 17,
  "flow_per_hour": 1020.0,
  "small_vehicle_flow": 14,
  "large_vehicle_flow": 3,
  "avg_speed_kmh": null,
  "avg_occupancy_pct": 29.6,
  "avg_queue_length_m": 0.8,
  "max_queue_length_m": 11.5,
  "queue_duration_sec": 3.0,
  "max_queue_duration_sec": 1.0,
  "lane_count": 2,
  "lanes": [ { "lane_no": 1, "flow": 12, "...": "..." },
             { "lane_no": 2, "flow": 5,  "...": "..." } ],
  "in_flow": 5,
  "out_flow": 3
}
```

| 欄位 | 說明 |
|---|---|
| `detector_id` | 偵測器名稱 |
| `road_name` | 道路名稱 |
| `status` | `active` 正常運作／`disabled` 該偵測器停用 |
| `time_start` / `time_end` | 本筆涵蓋的時間範圍 |
| `direction` / `direction_label` | 行車方向代碼／中文 |
| **`total_flow`** | **本時段通過的車輛數** |
| `flow_per_hour` | 換算成每小時車流率 |
| `small_vehicle_flow` | 小型車（小客車、小貨車、機車…） |
| `large_vehicle_flow` | 大型車（大貨車、大客車、聯結車） |
| `avg_speed_kmh` | 平均車速 |
| `avg_occupancy_pct` | 平均佔用率（%） |
| `avg_queue_length_m` / `max_queue_length_m` | 平均／最大排隊長度（公尺） |
| `queue_duration_sec` / `max_queue_duration_sec` | 排隊累計／單次最長時間（秒） |
| `lane_count` / `lanes[]` | 車道數與各車道明細 |
| **`in_flow` / `out_flow`** | **進場／出場車輛數**（僅部分偵測器有，見下） |

### `stats` — 統計摘要

| 欄位 | 說明 |
|---|---|
| `record_count` | `records` 的筆數 |
| `detector_count` | 偵測器數量 |
| `bucket_count` | 時間段數量（本模式恆為 `1`） |
| `overall` | 全部偵測器加總 |
| `by_detector[]` | 各偵測器小計 |
| `peak_bucket` | 本次車流最高的偵測器 |

> `record_count = detector_count × bucket_count`。
> 本模式只有一個時間段，所以 `records` 與 `by_detector` 數字相同，
> 取任一個即可；`records` 的欄位比較完整。

---

## 四、判讀時要注意的六件事

### 1. `total_flow` 不要加上 `in_flow` / `out_flow`

三者是**各自獨立**的計數，相加會重複計算。

| 事件 | 計入 `total_flow` | 計入 `in_flow`／`out_flow` |
|---|---|---|
| 一般通過偵測區 | ✅ | ❌ |
| 駛入進出框 | ❌ | ✅ `in_flow` |
| 駛離進出框 | ❌ | ✅ `out_flow` |

`total_flow` 本身已是完整車流量。

### 2. 沒有 `in_flow` 欄位 ≠ 進出量是 0

只有**設定了進出線**的偵測器才會有這兩個欄位。

| 情況 | 意思 |
|---|---|
| 沒有 `in_flow` 這個欄位 | 該偵測器不做進出計數 |
| `in_flow: 0` | 有做進出計數，但這段時間沒有車進出 |
| `in_flow: 5` | 5 台車進場 |

判斷方式：檢查**欄位是否存在**，而不是看值是否為 0。

### 3. `null` 代表「沒有量到」，不是 0

- `avg_speed_kmh: null` → 該偵測器**未設定測速**
- `avg_speed_kmh: 0` → 車速真的量到 0（車輛靜止）

這兩者意義完全不同，請勿把 `null` 當成 0 處理。排隊長度等欄位同理。

### 4. `status: disabled` 的偵測器數值恆為 0

該偵測器目前停用中，`total_flow: 0` 不代表沒有車。
偵測器清單固定，不會因為停用就從回應中消失。

### 5. 短時間內的進出量可能不對稱

`in_flow` 與 `out_flow` 在短時間內不一定相等 —— 車輛可能在上一分鐘進場、
這一分鐘才出場。時間拉長就會趨於平衡（實測 30 分鐘內進出比為 1.00）。

### 6. 分鐘剛開始時 `flow_per_hour` 會劇烈跳動

`flow_per_hour` 是用 `elapsed_sec` 外推的。分鐘剛開始（例如 `elapsed_sec: 3`）
時一台車就會讓數值跳到很大。**建議 `elapsed_sec` 大於 20 再顯示這個欄位。**

---

## 五、常見問題

**Q：每 20 秒查一次，數字會重複計算嗎？**
不會。同一分鐘內的三次查詢是「同一分鐘的累積進度」，不是三段各自獨立的資料。
若要存檔，每分鐘只取最後一次（或改用 `vd-report/latest`）。

**Q：可以指定只查某一台嗎？**
可以，加上 `detector_id`：
```
/api/v1/external/realtime?mode=minute&detector_id=2
```

**Q：查詢頻率有上限嗎？**
每分鐘 120 次。20 秒查一次等於每分鐘 3 次，遠低於上限。

**Q：回應多快？**
實測 20～40 毫秒。

**Q：時間是什麼時區？**
回應中的時間皆為台北時間（`+08:00`）。

**Q：想要每分鐘的最終統計值，不要中途進度？**
改用：
```
/api/v1/external/vd-report/latest?minutes=5&interval=1m
```
回傳最近 5 個**已結束**的分鐘，不重疊，適合存進資料庫
（以 `detector_id` + `time_start` 為唯一鍵）。

---

## 六、錯誤回應

| HTTP | 意思 | 處理方式 |
|---|---|---|
| `401` | 金鑰缺少或無效 | 檢查 `X-API-Key` |
| `403` | 金鑰無此項目權限 | 聯繫系統管理者 |
| `422` | 參數格式錯誤 | 檢查 `mode` 只能是 `minute` 或 `window` |
| `429` | 超過查詢頻率上限 | 降低查詢頻率 |
