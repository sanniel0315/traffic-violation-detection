---
marp: true
title: 停車場車位自動管理系統
---

# 停車場車位自動管理系統

### 邊緣 AI · 自動偵測 · 自我學習

NVIDIA Jetson AGX Orin 邊緣運算
Client / Server 架構 · 無雲端依賴

製作日期：2026-06-18

---

## 一句話定位

把「需要人盯著看、定期人工校正」的停車場監看，

升級成 **自己偵測、自己仲裁、自己學習進化** 的邊緣 AI 系統。

> 單台 Jetson 完成：偵測 → 佔用判定 → 進出計數 → 自我學習 → 對外服務

---

## 痛點

- 傳統車位偵測模型 **換場景就不準**（PKLot 在台灣場景全判空）
- 低解析度（640×480）小車易漏抓 → **假空位**
- 人工標訓練資料 **逐格點選太累**、難維護
- 車格 ROI 靠人工/估算 **常沒貼合實際停車格**

---

## 解決方案總覽

| 層面 | 作法 |
|------|------|
| 看得到 | YOLO 即時偵測 + 逐格佔用判定 |
| 算得準 | 分類器補漏 + 時間遲滯 + VLM 仲裁 + 交叉校正 |
| 自己學 | 自動收集 → SAM 精準標註 → 訓練 → 品質閘門 → 自動部署 |
| 貼得合 | SAM 遮罩從實車重生車格 ROI |

---

## 系統架構（Client / Server）

```
影像來源(TwiPcam/Frigate)
        │
   Jetson 邊緣伺服器  ── traffic-api(:8000) REST
   ├ 偵測引擎: YOLO + 逐格分類器 + 遲滯 + VLM
   ├ 自我改進: collector(每150s) + retrain(每日14:00)
   └ 資料: SQLite
        │  HTTP / JSON
   Client: 儀錶板 / 車位編輯器 / 標註評測 / VLM 助理 / 外部介接
```

- 程序隔離：OCR、IO、Frigate 各自獨立 systemd 服務，單一崩潰不拖垮全系統

---

## 核心：多層防誤偵測

對每個車格多邊形：

1. **YOLO 主判** — 車輛中心落格內 / 車框 IoU
2. **分類器補漏** — 本地 fine-tune 模型補 YOLO 漏抓的車（修假空位）
3. **時間遲滯** — 單幀漏抓不翻空（空→占 2 幀、占→空 3 幀），防抖
4. **VLM 仲裁** — 邊界車格背景 Qwen2-VL-2B 複判（參考）
5. **交叉校正** — 區域內車數 vs 佔用數，抓系統性偏差

> 設計哲學：**空位判定寧可慢、求準**（謊報空位＝最糟體驗）

---

## AI 亮點 1：SAM 精準標註

開源 **Segment Anything** 導入（MobileSAM on Jetson）

- YOLO 偵測車框 → SAM 轉**精準遮罩** → 「遮罩 ∩ 車格面積比」判佔用
- 遮罩不像方框會溢到鄰格

### 成效（實測）

> **自動標註模糊率：45% → 2.6%**

標籤更乾淨 → 分類器更準

---

## AI 亮點 2：車格從實車重生

問題：原車格靠 Grid 估算，斜角停車對不齊、還把**行車道誤畫成車格**

解法：取最滿幀 → SAM 遮罩每台車最小旋轉矩形（貼合斜角）→ 跨幀去重

### 成效

> **20 個錯位車格 → 15 個貼合實車的車格**（自動，目視驗證紅綠正確）

---

## AI 亮點 3：自我學習管線（全自動）

```
持續收集多時段畫面
   → SAM 精準自動標註（無人工）
   → 訓練逐格分類器
   → 品質閘門（防退化模型）
   → 通過才部署 + 重啟，否則自動回滾
```

- `parking-collector`：每 150s 抓幀、滾動保留 800 張
- `parking-retrain`：每日 14:00 自動重訓
- **模型隨場景越用越準，無需人工介入**

---

## 品質閘門（防呆）

部署前自動驗證，**擋下退化模型**：

- 與強偵測器一致性比對（占用比例差 < 門檻）
- 在「有車的幀」要抓到佔用（擋「永遠判空」）
- 未過 → **自動回滾**舊模型，不部署

> 實例：曾擋下一個「過度判占用」的候選模型，production 維持安全版

---

## 準確度驗收

| 指標 | 通過門檻 |
|------|---------|
| 車格對齊率 | ≥ 95% |
| 空位 precision（說空就真空） | ≥ 0.95 |
| 佔用 recall（有車有抓到） | ≥ 0.90 |
| 整體 accuracy | ≥ 0.92 |
| 交叉校正差距 | ≤ 2 |

工具：預測上色目視圖、人工標註評測（precision/recall/混淆矩陣）、端到端對照

---

## 部署與維運

- **自動部署**：git push → GitHub Actions（Jetson runner）→ 15 項驗證 → 自動上線（約 2 分鐘）
- **systemd 服務**：traffic-api / parking-collector / parking-retrain / traffic-io
- **時間同步**：systemd-timesyncd（Stratum 1），確保時間戳正確
- **穩定性**：程序隔離，近期 0 重啟 0 崩潰

---

## 技術棧

- **邊緣硬體**：Jetson AGX Orin（CUDA 12.2 / TensorRT 8.6）
- **AI**：YOLOv8n / yolo11m（標註）/ yolov8n-cls（逐格）/ MobileSAM / Qwen2-VL-2B
- **Server**：FastAPI · SQLite · systemd
- **Client**：Vue 3 SPA · Element Plus · Chart.js（支援中英切換）
- **影像**：TwiPcam HTTP · Frigate + go2rtc

---

## 價值總結

- **準**：多層防誤 + SAM 精準標註 + 貼合實車 ROI
- **自主**：邊緣單機、無雲端、資料留現場、低延遲
- **進化**：自動收集→標註→訓練→部署，越用越準
- **可驗收**：明確指標門檻 + 自動化驗收工具

### 停車場管理，從「靠人盯」到「自己會」

---

## 附：可現場展示項目

1. 即時車位總覽（佔用/空位/佔用率）
2. 車位編輯器（拖框 / Grid / **SAM 點選**）
3. AI 視覺助理（語音問「現在幾個空位」）
4. 預測上色驗證圖（紅=占 / 綠=空，目視核對）
5. 夜間自動重訓 log（自我學習證據）
