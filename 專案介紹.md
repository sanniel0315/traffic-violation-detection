# 🚦 交通違規影像分析系統 — 專案介紹

> **AI 邊緣運算 × 智慧交通監控 × VD 車道報表**
>
> 基於 NVIDIA Jetson 平台的一站式交通影像分析方案，整合車輛偵測、車牌辨識、違規與壅塞分析，並產出 VD 車道級報表供路口管理與交控決策使用。

---

## 📌 專案定位

| 項目 | 說明 |
|------|------|
| **平台** | NVIDIA Jetson Xavier NX（JetPack 6.0 / CUDA 12.2 / TensorRT 8.6） |
| **後端** | FastAPI（Port 8000）+ SQLAlchemy + SQLite |
| **前端** | Vue 3 SPA + Element Plus（含 EZ Pro 風格 NVR 回放介面） |
| **AI 模型** | YOLOv8n（車輛偵測，TensorRT 加速）、YOLO26s-cls（大型車細分類，Top-1 97.7%）、Tesseract（車牌 OCR） |
| **NVR 整合** | Frigate + MQTT，多攝影機事件錄影與動態偵測 |
| **部署型態** | Docker 一鍵部署、可離線打包 image 上板 |

---

## ✨ 系統特色

### 1. 🚗 多階段車輛偵測（Two-Stage Detection）

- **第一階段**：YOLOv8n + TensorRT，偵測 6 類交通參與者（汽車、機車、公車、卡車、自行車、行人）。
- **第二階段**：偵測到 truck / bus 時自動觸發 **YOLO26s-cls 大型車細分類**：
  - 大貨車（heavy_truck，等效長度 12 m）
  - 小貨車（light_truck，等效長度 6 m）
  - 大客車（bus，等效長度 12 m）
- **訓練成果**：Val Top-1 **97.74%** / Test Top-1 96.93%，推論僅 1.7 ms/張。
- **效益**：使排隊長度與車流結構估算更貼近真實車種混合。

### 2. 🔢 台灣車牌辨識（LPR）

- 支援多種台灣車牌格式（`ABC-1234` / `AB-1234` / `1234-AB` / 機車三碼…）。
- **6 種預處理策略** 並行推論：CLAHE、Otsu、自適應二值化、銳化、反轉、透視校正。
- 可上傳單張、攝影機截圖辨識、亦可開**串流 LPR 視覺化**即時掛載車牌。

### 3. 🚨 違規規則引擎

涵蓋：闖紅燈、超速、違規停車、逆向行駛、未戴安全帽、騎樓違停。基於 ROI + 車輛追蹤器（VehicleTracker）做多幀關聯，避免單幀誤判。

### 4. 🚦 即時壅塞偵測

- **演算法**：YOLOv8 偵測 → ROI 過濾 → 車輛佔用面積 / ROI 面積 → 10 幀移動平均平滑。
- **四級壅塞等級**：

  | 等級 | 佔用率 | 說明 |
  |------|--------|------|
  | low | < 20% | 暢通 |
  | medium | 20–40% | 中等 |
  | high | 40–60% | 擁擠 |
  | critical | > 60% | 嚴重壅塞 |
- **排隊長度估計**：依車種等效長度 + 1.5 m 安全間距累加。

### 5. 📹 NVR 整合 + EZ Pro 風格回放介面

- Frigate NVR 動態偵測 + 事件錄影 + MQTT 推送。
- Web 端內建 **EZ Pro 深色主題回放頁**：1×1 / 2×2 / 3×3 多格分割、Resource Tree 拖放、事件時間軸標記、Playhead、書籤、0.5×–8× 變速。
- 快捷鍵：`Space` 播放／暫停、`B` 加書籤、`←` `→` 快進/後退 5 秒。

### 6. 🔐 帳密 + RBAC 權限派放

- HttpOnly Cookie Session。
- 三角色：`admin` / `ops` / `viewer`。
- **前台勾選即時生效**：側欄、頁面可見性同步更新；無權限自動導回可用頁。

### 7. 📊 系統日誌 + 即時連線狀態

FastAPI + WebSocket，攝影機連線、LPR、壅塞偵測等模組統一寫入，支援等級過濾（info / warning / error / success）。

### 8. 🛠 維運友善

- `scripts/init_dirs.sh` 自動處理 `data/` `models/` `output/` `storage/` 四個非版控目錄（支援外接 NVMe）。
- `scripts/smoke_check.py` + `restart_and_verify.sh` 部署後自動驗證 API 健康、VD 報表查詢、前端關鍵標記。
- `settings_backup.py` 可匯出／匯入設定，現場上板（無網路 Jetson）流程順暢。

---

## 📈 VD 車道報表（核心輸出）

VD（Vehicle Detector）車道報表是本系統面向**交控中心、路口分析**最關鍵的對外產出。

### ▎報表頁面結構

前端獨立頁面：**`🛣️ VD 車道報表`**

#### 查詢條件

| 條件 | 選項 |
|------|------|
| 時間刻度 | 近 1 小時 / 近 6 小時 / 近 24 小時 / 近 7 天 |
| 顆粒度 | **每分鐘 / 每 5 分鐘 / 每小時** |
| 快捷鈕 | 今日 / 近 24h / 近 7 天 |
| 設備 | 全部設備 / 指定 `camera_id` |
| 方向 | 全部 / 指定方向 |
| 起訖時間 | 任意 datetime 範圍 |
| 最小總流量 | 過濾低流量列 |

#### 報表欄位（雙列表頭）

**通用欄位**（每列固定）

- 設備編號 / 道路 / 時間 / 方向 / 車道數

**「總」彙總欄**

- 總流量
- 平均速度
- 平均占量 (%)
- 最大估計排隊長度 (m)
- 平均估計排隊長度 (m)
- 排隊持續時間

**「分車道」欄**（依該攝影機實際車道數動態展開，每車道 5 欄）

- (n) 總流量
- (n) 平均速度
- (n) 平均占量 (%)
- (n) 小車流量
- (n) 大車流量

#### 匯出能力

- ✅ 匯出 **CSV**（沿用聚合查詢結果，不重算）
- ✅ 匯出 **PDF**（標題：`VD 車道報表`）
- 📄 對外 API：`GET /api/external/vd-report`（需 API Key + scope `vd_report`），同時提供 CSV 下載端點。

---

### ▎API 規格

#### 內部 API

```http
GET /api/traffic/vd-report
  ?camera_id=<int>
  &start_time=<ISO8601>
  &end_time=<ISO8601>
  &bucket_size=1m | 5m | 1h
```

回應範例：

```json
{
  "bucket_size": "5m",
  "camera_id": 62,
  "start_time": "2026-04-27T00:00:00+00:00",
  "end_time":   "2026-04-27T01:00:00+00:00",
  "aggregation": { "traffic_5m": 12 },
  "items": [
    {
      "deviceId": 62,
      "roadName": "中山路",
      "timeText": "2026-04-27 08:05",
      "directionText": "南向",
      "laneCount": 3,
      "totalFlow": 184,
      "avgSpeed": 42.6,
      "avgOccupancyPct": 18.3,
      "maxQueueLengthM": 26.0,
      "avgQueueLengthM": 12.4,
      "queueDurationSec": 95,
      "lanes": {
        "1": {"flow": 78, "avgSpeed": 41.2, "avgOccupancyPct": 17.5, "smallFlow": 70, "largeFlow": 8},
        "2": {"flow": 62, "avgSpeed": 43.0, "avgOccupancyPct": 18.9, "smallFlow": 58, "largeFlow": 4},
        "3": {"flow": 44, "avgSpeed": 43.5, "avgOccupancyPct": 18.4, "smallFlow": 42, "largeFlow": 2}
      }
    }
  ]
}
```

#### 對外 API（含 API Key）

```http
GET  /api/external/vd-report?detector_id=62&start_time=...&end_time=...&bucket=5m
GET  /api/external/vd-report.csv?...
Header: X-API-Key: <key>   (scope: vd_report)
```

---

### ▎效能設計：實體聚合表（不只是 view）

VD 報表大量範圍查詢（例如近 7 天每分鐘）若直接掃 `traffic_events`，**35 萬筆 rebuild 約 16 秒**。系統採增量聚合策略：

```text
traffic_events                               ← 原始明細（保留追查）
congestion_samples
        │
        ▼  每分鐘增量聚合（aggregation_job_state 記錄 last_processed_at）
traffic_report_agg_1m   congestion_report_agg_1m   lpr_report_agg_1h
        │
        ▼  滾動彙總
traffic_report_agg_5m   traffic_report_agg_1h
congestion_report_agg_5m   congestion_report_agg_1h
        │
        ▼
   報表 API / 匯出 API
```

#### 聚合公式

| 指標 | 計算方式 |
|------|----------|
| `total_flow` | bucket 內 `traffic_events` 筆數 |
| `avg_speed` | bucket 內有效 `speed_kmh` 平均 |
| `avg_occupancy` | bucket 內 `occupancy` 平均 |
| `small_vehicle_flow` | `car` + `motorcycle` 事件數 |
| `large_vehicle_flow` | `bus` + `truck` 事件數 |
| `max/avg_queue_length_m` | 同 bucket 壅塞樣本的最大/平均 |
| `queue_duration_sec` | bucket 內 `queue_active=true` 累計秒數 |

#### 排隊長度等效公式

```text
排隊長度 ≈ Σ(各車等效長度 + 安全間距 1.5 m)

motorcycle = 2 m
car        = 6 m
bus        = 12 m
truck      = 12 m
```

#### Cache 策略（API 層）

- 路徑：`api/routes/traffic.py:22`
- TTL：60 秒，相同 `(start, end, bucket, camera_id)` 範圍內**跳過 rebuild**，直接讀已有聚合表。
- SQLite 鎖：`PRAGMA busy_timeout=5000` + 指數退避重試。

#### Fallback 策略

- 聚合表尚未有資料時：
  - 小範圍允許回退掃 raw table。
  - 大範圍直接回 `aggregation_pending`，避免前端誤判為 0。

---

### ▎資料正確性提醒

- 「平均占量」需事件含 `occupancy`，來源未提供時報表顯示 `-`，**不以 0 充數**。
- 攝影機命名 `<camera_id>_<lane_id>`（例：`62_1`），車道欄位由此自動展開。
- 報表格式欄位 **固定不變**，所有效能優化都在後端資料來源切換完成，前端與外部串接零侵入。

---

## 🧭 文件導覽

| 文件 | 用途 |
|------|------|
| `README.md` | 系統總覽、安裝部署 |
| `RUNBOOK.md` | 運維手冊 |
| `API整合文件.md` / `docs/API_REFERENCE.md` | 完整 API 規格（95+ 端點） |
| `DB_ERMODEL.md` | 資料庫 ER Model |
| `報表聚合表設計.md` | **VD / 壅塞 / LPR 聚合表設計細節** |
| `ocr 流程.md` | 車牌 OCR pipeline |
| `推送流程.md` | GitHub 推送 + 離線 image 部署 |

---

*本介紹文件最後更新：2026-04-27*
